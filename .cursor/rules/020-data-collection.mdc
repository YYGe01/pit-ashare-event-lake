---
description: quant_data_center 数据采集和回补实现规则。
globs: ["src/quant_data_center/collectors/**/*.py", "src/quant_data_center/storage/**/*.py", "src/quant_data_center/jobs/**/*.py", "config/**/*.yaml"]
alwaysApply: false
---

# QDC 数据采集规则

- 回补链路应同时维护控制任务、raw JSON、bronze Parquet、silver 表和 `qdc_meta.source_object` 登记。
- provider、dataset、目录和 universe 配置放在 `config/quant_data_center.yaml`。
- 当前优先迁移 AkShare A 股日频数据；新增付费源前必须先确认授权和凭据边界。
- 补采和历史导入必须保留真实系统采集时间。
- source 失败必须记录为任务错误或质量问题，不要静默吞掉。
- DuckDB 当前按单写进程使用，不要并行写同一个 `qdc.duckdb`。
