# 2026-04-27

## 政策监管官方列表 connector

- 目标：继续补免费 official/shadow source，把 `policy_regulatory_doc` 从 AkShare CCTV bootstrap 推进到官方列表源可手动对账。
- 修改：新增 `src/pitlake/connectors/policy/html_list.py` 通用 HTML 列表采集器，以及 `csrc.py`、`gov_cn.py`、`pbc.py` 三个具体 connector。
- 配置：`csrc_policy_news`、`gov_cn_policy`、`pbc_policy_news` 从 `planned` 改为 `active_shadow`，仍保持 `enabled: false`。
- 实现口径：只保存列表页 raw HTML、标题、URL、发布日期、部门和分类；不下载详情页、附件或正文。
- 测试：新增 `tests/test_policy_html_list_connectors.py`，覆盖 CSRC/gov.cn/PBC 三类页面的链接过滤、日期解析、字段标准化和 metadata 入账。
- 真实验证：`csrc_policy_news` 成功，`new_item_count=13`；`gov_cn_policy` 成功，`new_item_count=14`；`pbc_policy_news` 成功，`new_item_count=15`。

## SHFE 商品日频官方 connector

- 目标：继续补 P0 商品日频 official source，用上期所公开交易所 JSON 对 AkShare 商品样例源形成对账候选。
- 修改：新增 `src/pitlake/connectors/commodities/shfe.py`，使用 SHFE `/data/tradedata/future/dailydata/kxYYYYMMDD.dat` 接口采日交易快讯。
- 配置：`shfe_daily_commodity` 从 `planned` 改为 `active_shadow`，仍保持 `enabled: false`。
- 实现口径：保存交易所 raw JSON，并将 `o_curinstrument` 合约行标准化到 `commodity_daily`，字段包括合约、交易日、开高低收、结算价、前结算、成交量和持仓量。
- 测试：新增 `tests/test_shfe_daily_connector.py`，覆盖 SHFE JSON 字段标准化和 metadata 入账。
- 真实验证：运行 `pitlake run-source --source-id shfe_daily_commodity --end-date 20260424 --manifest-date 2026-04-27` 成功，过滤非合约行后 `new_item_count=332`、`quarantine_count=0`。
- 附带发现：Stooq 当前 CSV 下载端点要求通过官方页面/验证码获取 apikey，已将 `stooq_global_daily` 标记为 `planned_credential_required`，不绕过验证码。

## CZCE 和 GFEX 商品日频官方 connector

- 目标：继续补商品交易所 official source，覆盖郑商所和广期所。
- 修改：新增 `src/pitlake/connectors/commodities/czce.py`，使用 CZCE `/cn/DFSStaticFiles/Future/YYYY/YYYYMMDD/FutureDataDaily.txt` 静态文本文件采期货每日行情。
- 修改：新增 `src/pitlake/connectors/commodities/gfex.py`，使用 GFEX `/u/interfacesWebTiDayQuotes/loadList` JSON 接口采日行情。
- 配置：`czce_daily_commodity`、`gfex_daily_commodity` 从 `planned` 改为 `active_shadow`，仍保持 `enabled: false`；`dce_daily_commodity` 标记为 `planned_blocked_source_response`。
- 测试：新增 `tests/test_czce_daily_connector.py` 和 `tests/test_gfex_daily_connector.py`，覆盖文本/JSON 字段标准化和 metadata 入账。
- 真实验证：`czce_daily_commodity` 成功，`new_item_count=241`；`gfex_daily_commodity` 成功，`new_item_count=48`。
- 阻塞：DCE `publicweb/quotesdata/dayQuotesCh.html` 在本环境返回 HTTP 412，普通浏览器头也不放行；先不绕过，等待确认官方可访问路径。

## 默认对账范围扩展

- 目标：让新增 shadow/official source 能进入默认每日 reconciliation，而不是只停留在可手动采集。
- 修改：`DEFAULT_RECONCILIATION_DATASETS` 增加 `market_daily_ohlcv`、`commodity_daily` 和 `global_market_daily`。
- 修改：`active_shadow` disabled source 现在会作为候选 counterparty source 显示。
- 实现口径：日线按 `instrument + trading_date` 对齐；商品按 `exchange + contract + trading_date` 对齐；全球市场按 `symbol + trading_date` 对齐。
- 测试：扩展 `tests/test_ops_reconciliation_alert_backup.py`，覆盖 `market_daily_ohlcv` 的 close 差异对账。
- 修正：公告和政策源的不同官网 URL、部门和分类不再作为 critical mismatch，因为多官网转载同一文件时这些字段天然不同。
- 真实验证：运行 `pitlake reconcile --date 2026-04-27`，报告状态为 `warn`，`critical_finding_count=0`。

## Yahoo 全球市场 shadow 和 DCE connector 骨架

- 目标：继续收敛 P0 planned/shadow 缺口，把可低量验证的全球市场 shadow 源落地，同时让 DCE 已登记 adapter 不再是空占位。
- 修改：新增 `src/pitlake/connectors/global_markets/yahoo.py`，使用 Yahoo Finance chart JSON 低量采集 selected symbol 日频 bar，标准化到 `global_market_daily`。
- 配置：`yahoo_finance_global_daily` 从 `planned_shadow` 改为 `active_shadow`，仍保持 `enabled: false`，不进入默认 `run-enabled`。
- 修改：新增 `src/pitlake/connectors/commodities/dce.py`，实现 DCE `publicweb/quotesdata/dayQuotesCh.html` HTML 表格解析和 `commodity_daily` 标准化。
- 配置：`dce_daily_commodity` 继续保持 `planned_blocked_source_response`，原因是本环境真实访问仍是 HTTP 412；不绕过来源控制。
- 测试：新增 `tests/test_yahoo_global_connector.py` 和 `tests/test_dce_daily_connector.py`，覆盖请求参数、字段标准化、raw 写入和 metadata 入账。
- 真实验证：`yahoo_finance_global_daily` 成功，`new_item_count=1`；DCE 只做单元测试和配置占位，不把当前 412 阻塞伪装成成功。

## README 数据源覆盖表扩充

- 目标：按用户要求把 README 中 P0/P1/P2 数据源覆盖表从 logical_dataset 汇总粒度扩展到 source 粒度。
- 修改：替换 `README.md` 的 P0/P1/P2 覆盖表，每个 `logical_dataset` 下每个已登记 source 单独一行。
- 新增字段：来源/费用、是否已启用、未启用原因/缺口、对账状态。
- 口径：以 `config/source_registry.yaml`、已有运行决策和当前 reconciliation 状态为事实来源；明确区分 enabled 主流程、disabled active_shadow、planned credential/entitlement/blocker。
- 验证：未运行测试；本次只修改文档。

## 本地质量、健康和重试增强

- 目标：阅读 README 后，先完成当前可在本地闭环的未完成项；不处理需要授权、外部凭据、生产调度或 7-30 天观察的事项。
- 修改：扩展 `quality-report`，新增 `quality_findings`，覆盖 dataset contract 必填字段缺口、未知字段漂移、基础行情异常值；新增 `--strict-coverage` 检查 enabled source 当天覆盖。
- 修改：新增 `pitlake health-report`，按 `schedule_policy.yaml` 的 freshness SLO 评估 enabled source，并写入 SQLite `source_health` 表。
- 修改：`run-source` / `run-enabled` 新增 `--max-attempts` 和 `--retry-backoff-seconds`，只重试 connector 未捕获异常。
- 测试：新增 `tests/test_health_report.py`、`tests/test_connector_runner_retry.py`，扩展 `tests/test_quality_report.py`；相关测试已通过。
- 文档：更新 `README.md` 和 `docs/v0_runtime_decisions_zh.md`，把质量规则和本地 SLO/retry 从“未完成”调整为“本地 bootstrap 已完成，生产级仍需连续运行校准”。
- 后续调整：按用户反馈去掉 `source_id` 列，新增 `中文名称` 列；具体 source 以 provider/API/官网来源写入 `数据源/费用` 列。
