# 2026-05-12

## 训练数据源完整性评估

- 新增 `docs/训练数据源完整性评估与改进事项.md`，用于记录当前 QDC 数据资产相对 Qlib 训练的可用性、缺口和后续优先级。
- 结论：当前链路足够做 QDC 到 Qlib 的数据层 smoke 和 baseline 接线验证，但不够支撑严肃训练结论。
- 日期判断：`2025` 行情不是上游缺失，抽样 `SH600000`、`SZ000001` 在 `2025-01-01` 到 `2025-12-31` 各有 243 行；当前缺口来自回补和导出范围尚未覆盖。
- 新闻判断：当前 AkShare 新闻源只适合作为近实时补位，不适合作为完整历史新闻库；正式训练需要授权历史新闻源。
- 公告判断：当前公告源可做标题级公告因子，但完整训练底座应接 CNINFO、交易所公告或 Tushare `anns_d`，并保存 PDF、hash、披露时间和跨源校验信息。
- 更新 `docs/迁移实施计划.md`，增加该评估文档的索引和后续 P0-P3 改进顺序。

## 训练数据源评估验证记录

- 该轮为文档改动，未运行 `pytest` 或 `ruff check .`。
- 已执行 `git status --short` 检查工作区状态。

## 每日自动采集流水线

- 环境：按用户要求使用 `ai-trader`，已执行 `conda run -n ai-trader python -m pip install -e ".[market,dev]"`，补齐 `duckdb`、`akshare`、`ruff` 和当前项目 editable 安装。
- 新增 CLI：`qdc daily-pipeline`，默认 `all_a` 全市场 active 标的，串联 `stock_basic` 刷新、单日 daily 采集、`build-factors`、`sync-parquet`、`quality` 和 `export-qlib`。
- 扩展 CLI：`qdc daily --all-market --refresh-stock-basic` 可复用 `stock_basic` 作为全市场标的来源。
- 新增文档：`docs/每日自动采集实施计划.md`，记录运行环境、收盘后 18:30 调度建议、Windows 计划任务、失败恢复和当前边界。
- 数据源判断：当前真实采集仍只有 AkShare；公告为标题/URL 级，新闻为近实时补位源，尚不足以支撑严肃非结构化训练结论。
- 验证：`conda run -n ai-trader qdc validate-config` 成功；`conda run --no-capture-output -n ai-trader pytest` 成功，37 个测试通过；`conda run -n ai-trader ruff check .` 成功；`conda run -n ai-trader qdc daily-pipeline --date 2026-05-11 --symbols "SH600000,SZ000001" --batch-size 1 --control-only` 成功。

## 非结构化数据每日爬虫调研

- 新增 `docs/量化非结构化数据每日爬虫实施方案.md`，用于评估爬虫是否适合解决每日新增公告、新闻和舆情数据积累。
- 调研结论：爬虫适合解决每日新增公告和公开新闻补位，不适合作为多年历史新闻训练集的替代品。
- 推荐方案：采用 QDC `crawler-lite`，即 source adapter、crawl_task 控制表、raw append-only 留档、parser、silver 文档表和日频因子，不在第一阶段直接引入完整 Scrapy 项目。
- Source 优先级：先接 CNINFO 公告，再接交易所公告补源，再接 1-2 个公开新闻源；社交舆情源暂不作为 P0。
- 合规边界：不绕过登录、验证码、付费墙，不保存 cookie/token/账号密码，对版权不明确的新闻正文默认 metadata_only。

## 爬虫控制面阶段 1

- 新增控制表：`qdc_meta.crawler_source`、`qdc_meta.crawl_task`、`qdc_meta.crawl_run`。
- 新增 registry：`src/quant_data_center/crawlers/registry.py`，第一阶段注册 `cninfo_announcement`。
- 新增 CLI：`qdc crawl-plan`、`qdc crawl-run`、`qdc crawl-daily`、`qdc crawl-list`、`qdc crawl-recover-running`。
- 当时边界：该阶段仅支持 `--control-only` 状态流；真实 CNINFO fetcher 和 parser 尚未接入。
- 验证：`conda run -n ai-trader qdc validate-config` 成功；`conda run --no-capture-output -n ai-trader pytest` 成功，42 个测试通过；`conda run -n ai-trader ruff check .` 成功；`conda run -n ai-trader qdc crawl-daily --date 2026-05-11 --control-only` 成功。

## CNINFO 公告列表真实抓取

- 新增 `src/quant_data_center/crawlers/sources/cninfo.py`，按日期调用 CNINFO 公告查询入口并解析公告列表。
- 当前写入：raw JSON、bronze Parquet、`qdc_silver.announcement` 最小字段；`url` 字段保存 CNINFO PDF URL。
- CLI：`qdc crawl-run` / `qdc crawl-daily` 支持真实 `cninfo_announcement`，并新增 `--page-size` / `--max-pages` 方便小范围 smoke。
- 边界：PDF 原文下载、PDF hash、正文抽取、公告类型标准化和交易所公告补源尚未实现。
- 验证：`conda run -n ai-trader qdc validate-config` 成功；`conda run --no-capture-output -n ai-trader pytest` 成功，43 个测试通过；`conda run -n ai-trader ruff check .` 成功；`conda run -n ai-trader qdc crawl-daily --date 2024-01-03 --source-id cninfo_announcement --page-size 2 --max-pages 1` 成功，写入 2 条公告。

## CNINFO PDF 原文留档

- 新增 `QdcObjectStore.put_bytes`，用于写入 PDF 等二进制原始对象，并登记 `qdc_meta.source_object`。
- 扩展 `qdc_silver.announcement` 兼容字段：`publish_time`、`source_record_id`、`source_sec_code`、`source_sec_name`、`adjunct_url`、`observed_at`、`collect_time`、`pdf_url`、`pdf_sha256`、`pdf_size_bytes`、`pdf_object_id`、`pdf_download_status`、`pdf_error_message`、`raw_object_id`、`parser_version`。
- CNINFO 爬虫默认下载公开 PDF，PDF 以 `raw_file` 层登记，避免控制台 raw JSON 预览误解析二进制文件。
- 新增 CLI 参数：`--pdf-limit` 控制真实 smoke 下载数量，`--skip-pdf-download` 仅抓列表元数据。
- 定向验证：`conda run -n ai-trader python -m pytest tests/test_qdc_storage.py::test_qdc_crawl_plan_is_idempotent tests/test_qdc_storage.py::test_qdc_crawl_run_real_cninfo_with_fake_response` 成功，2 个测试通过；`conda run -n ai-trader ruff check src\quant_data_center\crawlers\sources\cninfo.py src\quant_data_center\storage\objects.py src\quant_data_center\storage\silver.py src\quant_data_center\storage\schema.py src\quant_data_center\cli.py tests\test_qdc_storage.py` 成功。
- 保护 PIT 字段：重跑公告时保留首次 `observed_at`，当新抓取未成功下载 PDF 时保留已有成功 PDF hash、size、object_id 和状态。
- 全量验证：`conda run -n ai-trader qdc validate-config` 成功；`conda run --no-capture-output -n ai-trader pytest` 成功，44 个测试通过；`conda run -n ai-trader ruff check .` 成功。
- 真实 smoke：`conda run -n ai-trader qdc crawl-daily --date 2024-01-04 --source-id cninfo_announcement --page-size 2 --max-pages 1 --pdf-limit 1` 成功，写入 2 条公告，下载 1 份 PDF，登记 3 个 source_object；`qdc quality --dataset announcement --start 2024-01-04 --end 2024-01-04` 检查 2 行，0 个 issue。


## 非结构化爬虫后续计划推进

- 新增 `sina_finance_news` 公开新闻 metadata-only 补位源，默认纳入 `crawl-daily`。
- 新增新浪财经滚动新闻爬虫：写 raw JSON、bronze Parquet，并按 `qdc_silver.stock_basic` 的 active 标的代码/名称把标题映射到 `qdc_silver.news`。
- 更新 README、迁移实施计划和非结构化爬虫实施方案，补充 `sina_finance_news` 命令与阶段 4 状态。
- 验证：`PYTHONPATH=src python -m quant_data_center.cli validate-config`、`pytest`、`ruff check .` 通过；`qdc validate-config` 因当前 shell 未安装 console script 不可用，`pip install -e .` 受网络 403 阻断。

## 运行环境口径统一

- 决策：项目统一使用已存在的 `ai-trader` conda 环境；不再创建或激活 `quant-data-center` conda 环境。
- 保留边界：`quant-data-center` 继续作为 Python 项目发行名，`quant_data_center` 继续作为包名、配置名和数据目录名。
- 更新：`environment.yml` 的环境名改为 `ai-trader`；`README.md`、`AGENTS.md` 和 `docs/迁移实施计划.md` 的环境相关命令统一改为 `ai-trader`。
- 验证：`conda run -n ai-trader qdc validate-config` 通过，`status=ok`；`conda run -n ai-trader pytest -q` 通过，45 passed；`conda run -n ai-trader ruff check .` 通过；`git diff --check` 通过。
