# 2026-04-28

## PitLake Console 阶段 2 继续实现

- 状态：2026-04-27 已完成阶段 1 MVP；本次继续补齐阶段 2 的只读 drilldown，并补上阶段 1 里尚未落地的 source x date 状态矩阵。
- 修改：`src/pitlake/ui/console_data.py` 新增 dataset items/coverage/quality/reconciliation、symbol universe、symbol detail、source matrix、manifest list/detail 和扩展搜索 payload。
- 修改：`src/pitlake/ui/server.py` 新增 `/api/datasets/{logical_dataset}/items`、`coverage`、`quality`、`reconciliation`、`/api/symbols`、`/api/symbols/{symbol}`、`/api/manifests` 和 `/api/manifests/{manifest_id}`。
- 修改：静态前端新增“股票”“对账”“Manifest”入口；首页新增 source x date 状态矩阵；dataset 详情新增 Explore、Coverage 和 Reconciliation；股票详情显示 registry sample symbols 口径下的 present/missing 覆盖和 raw 证据链。
- 文档：更新 `README.md`、`docs/v0_runtime_decisions_zh.md` 和 `docs/data_lake_frontend_console_design_zh.md`，记录当前控制台能力和股票缺失检查口径。
- 口径：股票缺失检查只基于 `source_registry.yaml` 的 registry sample symbols 和当天已观测 item；没有 security master / stock universe 前，不声明全市场缺失。
- 测试：扩展 `tests/test_ui_console_data.py`，覆盖 source matrix、symbol coverage、missing symbol、manifest detail 和 raw 搜索。
- 验证：`pytest tests/test_ui_console_data.py -q --basetemp .codex_tmp_ui_pytest2 -p no:cacheprovider` 通过，2 passed；`ruff check src/pitlake/ui tests/test_ui_console_data.py` 通过；`node --check src\pitlake\ui\static\app.js` 通过。

## PitLake Console 阶段 3 治理视图第一段

- 目标：继续阶段 3 的质量治理增强，先做只读可解释视图，不引入写操作、issue 状态流转或外部 BI。
- 修改：`src/pitlake/ui/console_data.py` 新增 `/api/governance` payload，包含 dataset quality score、近 30 天 volume baseline/anomaly、schema drift 汇总和 latest source_health 状态。
- 修改：`src/pitlake/ui/server.py` 接入 `/api/governance`；静态前端新增“治理”页，展示质量分、volume baseline、schema drift 和 source health 表格。
- 口径：quality score 是控制台运维评分，只用于采集层健康判断；volume baseline 需要至少 3 个历史观测日，否则标记 `not_enough_history`。
- 文档：更新 `README.md` 和 `docs/data_lake_frontend_console_design_zh.md`，记录阶段 3 已完成的只读治理能力和仍未实现的 issue 流转/告警链接/UI cache。
- 测试：扩展 `tests/test_ui_console_data.py`，覆盖 governance payload、source health missing 汇总、schema drift 和 volume anomaly。
- 验证：`pitlake validate-config` 通过；`pytest -q --basetemp C:\Users\73498\.codex\memories\pt -p no:cacheprovider` 通过，55 passed；`ruff check .` 通过；`node --check src\pitlake\ui\static\app.js` 通过。

## PitLake Console 阶段 3/4 收尾

- 目标：按用户要求检查“四个阶段”是否完成，并继续当前仓库内可安全落地的未完成项。
- 结论：阶段 1、阶段 2 已完成；阶段 3 原先只完成第一段；阶段 4 原先未落地。本次补齐只读范围内的阶段 3/4 能力，不引入写操作、外部 BI 服务或未授权全文展示。
- 修改：`src/pitlake/ui/console_data.py` 新增 `phase_status`、`tools` 和 `export` payload；`governance` 增加只读派生 issue 队列、issue summary、阶段状态；导出支持 `dataset_items`、`raw_objects`、`quality_findings` 的 JSON/CSV payload。
- 修改：`src/pitlake/ui/server.py` 新增 `/api/phase-status`、`/api/tools`、`/api/export`；静态前端新增“工具”页，展示四阶段状态、导出 API、搜索能力、UI cache、告警产物和 BI 连接提示；治理页新增只读 issue 表。
- 文档：更新 `README.md`、`docs/v0_runtime_decisions_zh.md` 和 `docs/data_lake_frontend_console_design_zh.md`，把阶段 3/4 状态调整为本地只读版本已完成，并明确 acknowledged/resolved 写入、外部 BI 服务和嵌入式 PDF viewer 延后。
- 验证：`conda run -n pit-ashare-event-lake pytest tests/test_ui_console_data.py -q --basetemp .codex_tmp_ui_pytest3 -p no:cacheprovider` 通过，3 passed；`conda run -n pit-ashare-event-lake ruff check src\pitlake\ui tests\test_ui_console_data.py` 通过；`node --check src\pitlake\ui\static\app.js` 通过。
