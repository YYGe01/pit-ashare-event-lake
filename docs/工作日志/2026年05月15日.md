# 2026年05月15日

## 2026-05-14 采集补齐和预览验收

- 停止旧控制台进程后补采 `2026-05-14` 东方财富滚动新闻，`eastmoney_roll_news` 成功写入 `document_count=1618`，覆盖 `946` 个标的。
- 补采 `2026-05-14` 东方财富研报 metadata，`eastmoney_research_report` 成功写入 `33` 行，覆盖 `31` 个标的。
- 补采 `2026-05-14` 东方财富公开舆情 metadata，`eastmoney_public_sentiment` 成功写入 `5182` 行，覆盖 `5182` 个标的。
- 互动问答源 `cninfo_investor_interaction` 需要按公司逐个访问互动易接口，不存在可用的全市场按日期查询接口；用户确认全量速度太慢后暂停本轮全量采集，只保留当前已采集结果。本轮暂停前未完成整源落库，因此 `2026-05-14` 互动问答当前为 `0` 行。
- 为互动问答 crawler 增加单标的问答页失败容错：某个公司问答页请求失败时记录 `question_failures` 并继续后续标的，避免单点网络错误拖垮整源。
- 重建 `2026-05-14` 因子：`daily_news_factor=946`、`daily_announcement_factor=843`、`daily_research_report_factor=31`、`daily_investor_interaction_factor=0`、`daily_public_sentiment_factor=5182`。
- `qdc sync-parquet --layer all` 成功写出 `stock_basic`、`announcement`、`news`、`research_report`、`public_sentiment` 和对应日频因子 Parquet。
- `qdc quality --start 2026-05-14 --end 2026-05-14` 检查 `21670` 行，`0` 个 issue。
- 控制台已启动在 `http://127.0.0.1:8765/`；`/api/daily-wide-preview?date=2026-05-14&mode=factor&limit=6000` 返回 `row_count=5520`、`hidden_count=0`，列包含 `news_count`、`announcement_count`、`research_report_count`、`investor_interaction_count` 和 `public_sentiment_count`。

### 采集耗时统计（不含互动问答）

统计口径：

```text
2026-05-15 手动补跑的 eastmoney_roll_news / eastmoney_research_report / eastmoney_public_sentiment 使用终端实测 wall time。
2026-05-14 原批次的 cninfo_announcement / sse_announcement / sina_finance_news 没有单源 wall time 日志，使用 qdc_meta.crawl_task.created_at -> updated_at 作为近似耗时。
cninfo_investor_interaction 已按用户要求暂停，不纳入本表。
```

| source_id | 数据集 | 2026-05-14 入库行数 | 覆盖标的数 | 本轮耗时 |
| --- | --- | ---: | ---: | ---: |
| `cninfo_announcement` | `announcement` | 1518 | 843 | 230 秒（约 3 分 50 秒） |
| `sse_announcement` | `announcement` | 802 | 453 | 406 秒（约 6 分 46 秒） |
| `sina_finance_news` | `news` | 0 | 0 | 587 秒（约 9 分 47 秒） |
| `eastmoney_roll_news` | `news` | 1618 | 946 | 167.5 秒（约 2 分 48 秒） |
| `eastmoney_research_report` | `research_report` | 33 | 31 | 36.7 秒 |
| `eastmoney_public_sentiment` | `public_sentiment` | 5182 | 5182 | 143.4 秒（约 2 分 23 秒） |

补充说明：`eastmoney_roll_news` 的控制表任务累计 `attempt_count=5`，早先失败尝试导致 `crawl_task.created_at -> updated_at` 跨天，不适合用于成功补跑耗时；上表使用成功补跑命令的终端 wall time。

## 互动问答采集优化实现

- 用户要求将不用代理的互动问答采集策略放入单独 Markdown 文档，同时实现优化并用小样本真实运行估算全市场耗时。
- 新增 `docs/互动问答采集优化方案.md`，单独记录 orgId/secid 缓存、源内标的并发、全市场保护阈值、日期早停、失败容错、推荐命令和不用代理的耗时评估方法。
- `cninfo_investor_interaction` 新增本地 orgId 缓存：`data/quant_data_center/cache/cninfo_investor_interaction_orgs.json`；缓存命中时跳过 `queryKeyboardInfo`。
- `crawl-daily` / `crawl-run` 新增 `--instrument-parallelism` 和 `--instrument-limit`；`--instrument-limit 0` 表示显式允许使用全部 active instruments，保留默认前 50 的误触发保护。
- 互动问答按标的内部并发执行，单个标的 question 请求失败不再中断整批；manifest 和任务结果会记录 request_count、org_cache 命中/更新数、org/question 失败数。
- 已增加单测覆盖 question 单标的失败容错、orgId 缓存命中和遇到早于目标日数据后的翻页早停。
- 不用代理真实耗时样本：本机 active instruments 为 5515；20 个混合标的冷启动 35.39 秒，缓存命中后 21.99-25.07 秒；50 个混合标的部分冷启动 65.56 秒，缓存命中后 44.60 秒；`instrument_parallelism=12` 为 44.79 秒，较 8 无明显改善。
- 基于 50 个缓存命中样本估算，全市场日更约 82 分钟；首次冷启动建缓存约 2.0-2.7 小时。该估算使用 `page_size=50`、`max_pages=1`，若放宽翻页会更慢。
