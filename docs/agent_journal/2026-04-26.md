# 2026-04-26

## V0 框架启动

- 目标：搭建第一版可用的 PIT 数据采集框架骨架，并在本地跑通一个真实 P0 采集路径。
- 修改：新增 `src/pitlake`、`config`、dataset contract、SQLite metadata store、raw append-only store、manifest generation、quality check、CLI 命令和测试。
- 验证：已运行 `pitlake validate-config`、`pitlake init`、`pitlake smoke-run`、`pitlake run-enabled`、`pytest` 和 `ruff check .`。
- 结果：`akshare_market_daily_ohlcv` 已能用 `akshare.stock_zh_a_daily` 成功采集默认三只样本股票的日线数据。
- 未解决问题：`akshare.stock_zh_a_hist` / Eastmoney 历史接口在当前本地网络下失败，后续应作为独立 shadow/fallback source，在网络验证后再接入。
- 建议下一步：先实现 `trading_calendar`，再实现 `price_limit` / `trade_status`。

## 数据流文档补充

- 目标：为初学者补充一份只看 Markdown 就能理解当前项目整体架构和数据流的说明。
- 修改：新增 `docs/project_data_flow_zh.md`，按阶段说明配置读取、运行创建、外部请求、raw 落盘、SQLite 记账、质量检查、标准观测项、manifest 发布和后续研究层读取。
- 样例：使用当前真实跑通的 `akshare_market_daily_ohlcv`，包含 `000001`、`600000`、`300750` 在 `2026-04-24` 的日线采集样例。

## 数据流文档增强

- 目标：把数据流文档从概览升级为字段级、源码调用级运行说明书。
- 修改：重写 `docs/project_data_flow_zh.md`，补充四个核心配置文件的字段含义、源码架构图、类装配关系、函数级调用链、raw/sidecar/SQLite/manifest 字段解释、`600000` 贯穿样例和后续架构变更同步规则。

## 数据流文档必要性说明

- 目标：补充每类保存文件和每个处理步骤的设计目的，说明省略后会出现的问题。
- 修改：扩展 `docs/project_data_flow_zh.md` 的文件说明表，并新增“每个处理步骤为什么需要”章节，覆盖 CLI、配置、source 加载、connector、raw、SQLite、质量检查、去重、manifest 和后续研究层读取。

## 数据流文档重复存储说明

- 目标：回应数据湖文件看起来重复的问题，聚焦说明每类保存文件的职责和重复边界。
- 修改：将 `docs/project_data_flow_zh.md` 第 27 节改为“保存文件的作用和重复边界”，说明 raw JSON、sidecar、SQLite、manifest 的区别，并删除上一轮新增的“每个处理步骤为什么需要”章节。

## 交易日历连接器

- 目标：按默认下一步实现 `trading_calendar`，让第二个 P0 source 进入同一条采集闭环。
- 修改：新增 `src/pitlake/connectors/market/akshare_calendar.py`，启用 `config/source_registry.yaml` 中的 `ashare_trading_calendar`，并补充 `tests/test_akshare_calendar_connector.py`。
- 数据源：使用 `akshare.tool_trade_date_hist_sina`，当前先记录 AkShare 返回的交易日，`calendar_id=cn_ashare`，`is_trading_day=true`。
- 文档：同步更新 `README.md`、`docs/v0_runtime_decisions_zh.md` 和 `docs/project_data_flow_zh.md`。
- 下一步：实现 `trade_status`，再实现 `price_limit`，然后让市场日线、交易日历、交易状态和涨跌停一起通过 `run-enabled` 稳定运行。

## 交易状态连接器

- 目标：继续默认下一步实现 `trade_status`，让第三个 P0 source 进入同一条采集闭环。
- 修改：新增 `src/pitlake/connectors/market/akshare_trade_status.py`，启用 `config/source_registry.yaml` 中的 `ashare_trade_status`，并补充 `tests/test_akshare_trade_status_connector.py`。
- 数据源：使用 `akshare.stock_tfp_em(date=YYYYMMDD)`，当前先记录返回的停复牌行，标准化为 `trade_status=halted`。
- 约束：该源不是全市场交易状态快照，正常交易状态、盘中临停细分状态和交易所官方对账后续再补。
- 修复：真实运行时 AkShare 可能返回空复牌日期 `NaT`，连接器已将可选日期解析失败统一降为 `None`，不影响必填字段入账。
- 验证：`pitlake validate-config` 通过，`pytest` 为 5 passed，`ruff check .` 通过；`run-enabled` 三个 enabled source 成功，`ashare_trade_status` 本次 `new_item_count=13`、`duplicate_count=14`。
- 文档：同步更新 `README.md`、`docs/v0_runtime_decisions_zh.md` 和 `docs/project_data_flow_zh.md`。
- 下一步：实现 `price_limit`，然后让市场日线、交易日历、交易状态和涨跌停一起通过 `run-enabled` 稳定运行。

## 涨跌停价格连接器

- 目标：继续默认下一步实现 `price_limit`，让第四个 P0 source 进入采集闭环。
- 修改：新增 `src/pitlake/connectors/market/akshare_price_limit.py`，启用 `config/source_registry.yaml` 中的 `ashare_price_limit`，并补充 `tests/test_akshare_price_limit_connector.py`。
- 数据源：使用 `akshare.stock_zh_a_daily` 查询目标日前若干天日线，取目标日前一条 `close` 作为 `prev_close`，按板块规则推算 `limit_up` / `limit_down`。
- 约束：当前是 V0 推算口径，尚未覆盖 ST、退市整理、上市初期无涨跌幅限制等特殊规则；后续应接官方或更完整 source 对账。
- 验证：`pitlake validate-config` 通过，`pytest` 为 6 passed，`ruff check .` 通过；`run-enabled` 四个 enabled source 成功，`ashare_price_limit` 本次 `new_item_count=3`。
- 文档：同步更新 `README.md`、`docs/v0_runtime_decisions_zh.md` 和 `docs/project_data_flow_zh.md`。
- 下一步：生成每日质量报告，并连续运行观察四个 enabled P0 source 的稳定性。

## P0 bootstrap 补齐

- 目标：按用户要求继续后续步骤，直到 V0/P0 数据集都有至少一个 enabled bootstrap source。
- 修改：新增每日质量报告 `pitlake quality-report`，并补齐 `adjustment_factor`、`announcement_index`、`policy_regulatory_doc`、`commodity_daily`、`global_market_daily` 的 AkShare bootstrap connector 和测试。
- 新增 source：`akshare_adjustment_factor`、`akshare_announcement_index`、`akshare_cctv_policy_news`、`akshare_commodity_daily`、`akshare_global_market_daily`。
- 验证：`pitlake validate-config` 通过，`pytest` 为 12 passed，`ruff check .` 通过；最终 `run-enabled` 为 success，`source_count=9`。
- 质量报告：已生成 `collection/quality_reports/dt=2026-04-26/quality_report_20260426T160001+0800.json`；状态为 `fail`，原因是当天早期调试失败 run 被如实纳入，不是最新 9 source run-enabled 失败。
- 约束：部分 source 仍是 V0 bootstrap 或推算口径，后续要补官方/影子源对账，尤其是复权因子、涨跌停、公告和政策监管。
## P0 对账、告警和备份

- 目标：按用户要求直接实现下一阶段的 `shadow/official source` 对账框架，以及外部告警和备份入口。
- 修改：新增 `pitlake reconcile`，默认覆盖 `adjustment_factor`、`price_limit`、`announcement_index`、`policy_regulatory_doc`；当只有单源时报告 `missing_counterparty_source`，当同一观察项有多源时比较关键字段差异。
- 告警：扩展 `pitlake alert`，默认写入 `data_lake/collection/logs/alerts.jsonl`，支持 `PITLAKE_ALERT_WEBHOOK_URL` 或 `--webhook-url` 发送 JSON webhook。
- 备份：新增 `pitlake backup`，备份 SQLite metadata、manifest、quality report、reconciliation report；支持 `PITLAKE_EXTERNAL_BACKUP_DIR`、`--target-dir` 和 `--include-raw`。
- 配置：`config/project.yaml` 预留 `paths.external_backup_dir`，不写入真实外部路径或凭据。
- 验证：新增 `tests/test_ops_reconciliation_alert_backup.py`，已用短路径 basetemp 运行通过；`ruff check .` 已通过。

## P1 财务指标 bootstrap

- 目标：回应“P0 是否完成，如果完成则继续 P1”，在保留 P0 稳定观察约束的同时，开始 P1 采集层 bootstrap。
- 判断：P0 已达到 bootstrap 闭环，9 个 P0 logical_dataset 均有 enabled source；但长期稳定完成还需要连续运行、对账、外部告警和备份。
- 修改：新增 `financial_indicator` dataset contract、`AkshareFinancialIndicatorConnector`、`akshare_financial_indicator` P1 source 和调度策略。
- 实现口径：使用 `akshare.stock_financial_analysis_indicator`，标准化 `instrument`、`exchange`、`report_date`、`period_type`，将 provider 返回的指标列保存到 `metric_payload`。
- 约束：P1 财务指标的真正 PIT 使用需后续结合公告/财报披露时间和修订版本对账。

## P1 bootstrap 扩展

- 目标：继续 P1 采集层开发，在不进入研究层的前提下，补齐低频公开数据 bootstrap 样例。
- 新增 enabled source：`akshare_macro_china_financial_credit`、`akshare_stock_capital_flow`、`akshare_industry_membership`、`akshare_concept_membership`。
- 修改：将 `macro_indicator`、`capital_flow`、`industry_membership`、`concept_membership` 接入 `config/source_registry.yaml` 和 `config/schedule_policy.yaml`。
- 实现口径：复用 `src/pitlake/connectors/p1_bootstrap.py` 的通用 DataFrame 采集模板；标准化基础 PIT 字段，provider 原始列进入 `metric_payload`。
- 测试：新增 `tests/test_p1_bootstrap_connectors.py`；同时让 P1/财务测试支持 `PITLAKE_TEST_ROOT`，用于 Windows 深路径下规避 raw 文件 260 字符限制。
- 验证：`pitlake validate-config` 通过；`pytest -q -p no:cacheprovider` 为 18 passed；`ruff check .` 通过。

## P1 GDELT 和天气 bootstrap

- 目标：继续完成 P1 剩余低成本公开源，优先接入文档中列出的 GDELT/全球新闻摘要和天气日频观测。
- 新增 enabled source：`gdelt_doc_global_event_summary`、`open_meteo_weather_daily`。
- 修改：新增 `GdeltDocArtListConnector` 和 `OpenMeteoWeatherDailyConnector`，接入 `global_event_summary`、`weather_daily` contract，并更新 provider/source registry 和 schedule policy。
- 实现口径：GDELT 只保存 DOC 2.0 `ArtList` 返回的文章元数据；Open-Meteo 只保存地点级 daily weather arrays；两者都不做事件抽取、情绪判断、实体链接或影响股票映射。
- 测试：扩展 `tests/test_p1_bootstrap_connectors.py`，覆盖 GDELT article metadata 和 Open-Meteo daily rows。
- 验证：`pitlake validate-config` 通过；`pytest -q -p no:cacheprovider` 为 20 passed；`ruff check .` 通过。真实运行 `open_meteo_weather_daily` 成功，`new_item_count=2`；真实运行 `gdelt_doc_global_event_summary` 成功，`new_item_count=20`。

## P1 资金行为 bootstrap 继续

- 目标：回答 P1 是否完成并继续剩余 P1 任务。结论是 P1 bootstrap 已覆盖多类数据，但严格意义上尚未完整完成；文档中剩余资金行为方向仍包括融资融券、北向和龙虎榜。
- 新增 enabled source：`akshare_margin_trading_detail`、`akshare_lhb_detail`、`akshare_hsgt_northbound_flow`。
- 实现口径：两者都写入 `capital_flow` logical_dataset，通过 `flow_scope` 区分 `margin_trading_sse`、`margin_trading_szse` 和 `lhb_detail`；provider 原始列完整进入 `metric_payload`。
- 验证：`akshare_margin_trading_detail` 真实运行成功，`new_item_count=2000`、`quarantine_count=1`；`akshare_lhb_detail` 真实运行成功，`new_item_count=76`、`duplicate_count=9`；`akshare_hsgt_northbound_flow` 真实运行成功，`new_item_count=2657`。
- 约束：龙虎榜、融资融券和北向资金当前是 AkShare bootstrap 口径，后续需要交易所/官方源、港交所源或付费源对账；基金持仓仍待后续继续。

## P1 bootstrap 可交付收口

- 目标：按用户要求重构 `docs/v0_runtime_decisions_zh.md` 的职责，并补齐 P1 剩余 bootstrap 缺口，让 P1 采集层达到可交付状态。
- 文档：将 `docs/v0_runtime_decisions_zh.md` 从流水账改为长期决策记录；普通代码更新只要求同步 `README.md` 和 `docs/agent_journal/`，只有范围、口径、运行策略或交付边界变化时才更新 runtime decisions。
- 新增 contract：`fund_holding`、`financial_news`、`public_sentiment`。
- 新增 enabled source：`akshare_fund_portfolio_hold`、`akshare_stock_news_main_cx`、`akshare_baidu_economic_news`、`akshare_stock_hot_rank`。
- 实现口径：基金持仓、财经新闻/经济事件和公开热度排行都只保存公开 provider 返回的原始指标/元数据到 `metric_payload`，不做事件抽取、情绪打分、持仓修订推断或研究层映射。
- 上游问题：`akshare_stock_news_em` 在本地 AkShare 1.18.57 真实调用会触发上游正则错误，已保留为 disabled planned source；P1 交付改用 `akshare_stock_news_main_cx`。
- 真实验证：`akshare_fund_portfolio_hold` 成功，`new_item_count=3463`；`akshare_stock_news_main_cx` 成功，`new_item_count=100`；`akshare_baidu_economic_news` 修正字段归一化后成功，`new_item_count=70`；`akshare_stock_hot_rank` 成功，`new_item_count=100`。
