# 2026-05-11

## quant_data_center 主线重构

- 目标：把仓库收敛为个人量化统一数据中心 `quant_data_center`，服务 Qlib 研究。
- 配置：保留 `config/quant_data_center.yaml` 作为唯一当前运行配置，定义数据根目录、DuckDB 路径、Parquet/QLib/logs 目录和迁移阶段策略。
- 代码：保留 `src/quant_data_center/` 包和 `qdc` CLI，当前支持 `validate-config`、`init`、`db-info`、`smoke`、`console`、`plan-backfill`、`list-backfill`、`run-backfill`、`refresh-universe`、`list-universe`、`daily`、`build-factors`、`sync-parquet`、`quality`、`export-qlib`、`verify-qlib`。
- 存储：新增 DuckDB 控制 schema `qdc_meta`，包含 `job_run`、`backfill_task`、`dataset_watermark`、`source_object`、`quality_issue`。
- Silver：新增 `qdc_silver.stock_basic`、`universe_constituent`、`trade_calendar`、`daily_bar`、`adj_factor`、`price_limit`、`trade_status`、`announcement`、`news`、`daily_news_factor`、`daily_announcement_factor`，并由 `SilverStore` 统一 upsert。
- 采集：AkShare 回补已支持 `stock_basic`、`trade_calendar`、`daily_bar`、`adj_factor`、`price_limit`、`trade_status`、`announcement`、`news`。
- 对象层：阶段 2 回补已写入 raw JSON、bronze Parquet，并在 `qdc_meta.source_object` 登记 content hash、文件大小和路径。
- Universe：`refresh-universe` 可写入 AkShare 指数成分快照；`plan-backfill` 优先使用最新快照，缺失时再回退到配置静态样例。
- Daily：`qdc daily` 可按日期和 universe 编排交易日历、行情、复权、涨跌停、交易状态、公告和新闻任务，并支持 `--control-only` 验证状态流。
- Retry：`qdc run-backfill --retry-failed` 可显式重试 `failed` 任务，默认 `run-backfill` 仍只运行 `pending` 任务。
- 因子：`qdc build-factors` 已支持 `news_v1`、`announcement_v1` 和 `all`，生成日频新闻/公告 count 因子。
- Parquet：新增 `QdcParquetSync`，可从 DuckDB silver 派生 silver Parquet 和 `gold/daily_research` Parquet。
- 质量：新增 `QualityChecker` 和 `qdc quality`，检查基础表空值、OHLC 区间、复权因子、涨跌停区间和交易状态枚举，并写入 `qdc_meta.quality_issue`。
- Qlib：新增 `QlibExporter` 和 `qdc export-qlib`，导出 day 频 calendars、instruments 和 features bin 文件，包含行情、vwap、复权、涨跌停和新闻/公告 count 因子；新增 `qdc verify-qlib`，通过本地 Qlib 读取导出的 provider 并输出严格 JSON。
- Console：新增 `qdc console` 本地只读 Web 控制台，直接读取 DuckDB 展示总览、回补任务、silver 数据预览、质量问题和 Qlib 导出索引。
- 清理：当前主线只保留 QDC 代码、配置、测试和文档；历史采集湖实现、配置、测试和长文档已从仓库主线移除。
- 注意：DuckDB 当前按单写进程使用；不要并行执行多个会写同一个 `qdc.duckdb` 的命令。

## 验证记录

- `conda run -n ai-trader pytest tests/test_qdc_storage.py` 通过，19 passed。
- `conda run -n ai-trader python -m ruff check src/quant_data_center tests/test_qdc_storage.py` 通过。
- `conda run -n ai-trader python -m quant_data_center.cli validate-config` 通过。
- `conda run -n ai-trader pytest` 通过，68 passed。
- `conda run -n ai-trader python -m ruff check .` 通过。
- `conda run -n ai-trader pytest tests/test_qdc_storage.py::test_qdc_run_backfill_can_retry_failed_tasks_control_only` 通过。
- `conda run -n ai-trader pytest tests/test_qdc_storage.py` 通过，23 passed。
- `conda run -n ai-trader pytest` 通过，23 passed。
- `conda run -n ai-trader python -m quant_data_center.cli verify-qlib --start 2024-01-02 --end 2024-01-02 --instruments SH600000,SZ000001 --provider-uri data/quant_data_center/qlib/cn_data` 通过。
- `conda run -n ai-trader python -m quant_data_center.cli export-qlib --start 2024-01-02 --end 2024-01-02 --provider-uri data/quant_data_center/qlib/cn_data` 通过，导出 26 个 Qlib 文件。
- `conda run -n ai-trader python -m quant_data_center.cli quality --dataset daily_bar --start 2023-01-01 --end 2024-12-31` 通过，检查 968 行，0 issue。
- `conda run -n ai-trader python -m quant_data_center.cli export-qlib --start 2023-01-01 --end 2024-12-31 --provider-uri data/quant_data_center/qlib/cn_data --market-name qdc_smoke` 通过，导出 2 个标的、484 个交易日、27 个 Qlib 文件。
- Qlib `Alpha158` handler 读取 QDC provider 成功，shape 为 `(968, 159)`，非空 label 为 964。
- `conda run -n ai-trader qrun config/qlib/workflow_config_lightgbm_alpha158_qdc_smoke.yaml` 通过，完成 LightGBM Alpha158 训练、`pred.pkl` artifact 和 SigAnaRecord。
- `conda run -n ai-trader pytest tests/test_qdc_storage.py::test_qdc_console_overview_reads_collection_state tests/test_qdc_storage.py::test_qdc_console_dataset_preview_filters_silver_rows tests/test_qdc_storage.py::test_qdc_console_dataset_preview_rejects_unknown_dataset -q` 通过，3 passed。
- `conda run -n ai-trader python -m ruff check src/quant_data_center/console.py src/quant_data_center/cli.py tests/test_qdc_storage.py` 通过。
- `node --check src/quant_data_center/console_static/app.js` 通过。
- `conda run -n ai-trader pytest` 通过，27 passed。
- `conda run -n ai-trader python -m ruff check .` 通过。
- `qdc console --host 127.0.0.1 --port 8765` 已启动；`curl /`、`curl /app.js`、`curl /api/overview` 和 `curl /api/dataset-preview?dataset=daily_bar&instrument=SH600000&limit=1` 均通过。

## 下一步

- 用真实大样本跑通 `refresh-universe`、`daily`、`build-factors`、`sync-parquet`、`export-qlib`。
- 用更长历史和更完整 universe 导出 Qlib provider 后，联调 Qlib Alpha158 baseline。
- 增加更细的数据质量规则、失败重试策略和大样本运行记录。

## 文本因子和 Qlib 外部 handler

- 决策：新闻、公告标题转数值因子的主体放在当前 QDC 仓库，不放 `/root/code/qlib`；Qlib 只消费导出的稳定日频字段。
- 新增：`src/quant_data_center/factor_engine/`，其中 `calendar_align.py` 按 `qdc_silver.trade_calendar` 把发布日期对齐到下一个开市交易日，`text_factors.py` 生成标题级情绪和事件规则因子。
- Schema：`daily_news_factor` 增加 `news_sentiment_mean`、`news_positive_count`、`news_negative_count`、`news_growth_count`、`news_risk_count`、`news_financing_count`；`daily_announcement_factor` 增加 `announcement_risk_count`、`announcement_financing_count`、`announcement_operation_count`，并提供缺列迁移。
- 导出：`qdc export-qlib` 现在会写出上述新闻/公告外部因子 `.day.bin` 文件，`gold/daily_research` 也同步包含这些列。
- Qlib：新增 `src/quant_data_center/qlib_ext/handlers.py` 的 `QdcAlpha158WithExternal`，继承 Qlib `Alpha158` 并追加 QDC 外部因子及 3/5 日均值表达式；新增 `config/qlib/workflow_config_lightgbm_alpha158_qdc_external.yaml`。
- 验证尝试：当前基础 shell 缺 `duckdb`，`pytest tests/test_qdc_storage.py::test_qdc_build_factors_aligns_text_titles_and_labels_events tests/test_qdc_storage.py::test_qdc_export_qlib_writes_day_provider_files -q` 未能收集测试；基础 shell 缺 `ruff`，`ruff check ...` 未运行。
- 验证：`conda run -n ai-trader pytest tests/test_qdc_storage.py::test_qdc_build_factors_aligns_text_titles_and_labels_events tests/test_qdc_storage.py::test_qdc_export_qlib_writes_day_provider_files -q` 通过，2 passed。
- 验证：`conda run -n ai-trader python -m ruff check src/quant_data_center/factor_engine src/quant_data_center/qlib_ext src/quant_data_center/storage/schema.py src/quant_data_center/storage/database.py src/quant_data_center/storage/silver.py src/quant_data_center/exports/qlib.py src/quant_data_center/storage/parquet.py src/quant_data_center/quality.py src/quant_data_center/factors.py tests/test_qdc_storage.py` 通过。
- 验证：`conda run -n ai-trader pytest` 通过，24 passed。
- 验证：`conda run -n ai-trader python -m ruff check .` 通过。
- 验证：`conda run -n ai-trader python -m quant_data_center.cli validate-config` 通过，`status=ok`。
- 验证：长假回看窗口调为 15 天后，`conda run -n ai-trader pytest tests/test_qdc_storage.py::test_qdc_build_factors_aligns_text_titles_and_labels_events -q` 和 `conda run -n ai-trader python -m ruff check src/quant_data_center/factor_engine/text_factors.py` 均通过。

## 真实 AkShare 小样本 smoke

- 目标：在 gitignored 的 `data/quant_data_center/` 中验证真实 AkShare 回补、daily 编排、因子、Parquet、quality 和 Qlib 导出链路。
- 命令：`qdc refresh-universe --universe csi300 --snapshot-date 2026-05-11` 成功，写入 300 条 `qdc_silver.universe_constituent`。
- 命令：`qdc daily --date 2024-01-02 --universe csi300 --symbols SH600000,SZ000001 --batch-size 2 --limit-tasks 20` 初次暴露 3 类真实源问题；修复后同一批失败任务可重试并成功。
- 修复：`stock_news_em` 上游正则异常改为记录 raw error 并返回 0 行，不再拖垮 daily。
- 修复：`stock_tfp_em` 中非 A 股代码会跳过；同一日期同一股票重复停牌记录按 first-wins 去重。
- 修复：公告列表重复行在 `SilverStore.upsert_announcements` 中按 `announcement_id` 去重。
- 命令：`qdc build-factors --factor-set all --start 2024-01-02 --end 2024-01-02` 成功，生成 454 条公告 count 因子，新闻因子 0 行。
- 命令：`qdc sync-parquet --layer all` 成功，写入 gold `daily_research` 2 行；随后 `sync-parquet --layer silver --dataset stock_basic` 成功，写入 5515 行证券主数据 Parquet。
- 命令：`qdc quality --start 2024-01-02 --end 2024-01-02` 成功，检查 2463 行，0 issue；`qdc quality --dataset stock_basic` 成功，检查 5515 行，0 issue。
- 命令：`qdc export-qlib --start 2024-01-02 --end 2024-01-02 --provider-uri data/quant_data_center/qlib/cn_data` 成功，导出 2 个标的、1 个交易日、26 个 Qlib 文件，包含 Alpha158 默认需要的 `$vwap`。
- Qlib 安装：`conda run -n ai-trader python -m pip install -e /root/code/qlib -i https://pypi.tuna.tsinghua.edu.cn/simple --extra-index-url https://mirrors.aliyun.com/pypi/simple` 成功，`qlib.__file__` 指向 `/root/code/qlib/qlib/__init__.py`，`pip check` 显示 No broken requirements found。
- 命令：`qdc verify-qlib --start 2024-01-02 --end 2024-01-02 --instruments SH600000,SZ000001 --provider-uri data/quant_data_center/qlib/cn_data` 成功，Qlib `D.calendar` / `D.list_instruments` / `D.features` 可读取 provider，返回 2 条 feature 行，包含 `$vwap`，`issues=[]`。
- 修复：`verify-qlib` 不再初始化 QDC DuckDB；缺失 provider、缺失 Qlib、缺失 instrument 或空 feature 会返回 `status=fail`；Qlib 初始化日志不污染 stdout；JSON 输出把 NaN/inf 规范化为 null。
- 当前本地 DB：`qdc db-info` 显示 `stock_basic=5515`、`universe_constituent=300`、`daily_bar=968`、`adj_factor=2`、`price_limit=2`、`trade_status=850`、`announcement=852`、`daily_announcement_factor=454`、`quality_issue=0`、`source_object=123`。
- 注意：一次并行执行 `sync-parquet` 和 `quality` 触发 DuckDB 文件锁；串行重跑通过。后续真实运行继续遵守单写进程规则。
- 下一步：扩大到完整 universe 后，补指数 benchmark / PortAnaRecord 并形成正式 Alpha158 baseline。

## Qlib Alpha158 data-layer smoke

- 目标：验证 QDC 导出的 Qlib provider 能被 Qlib `Alpha158` handler 直接消费，并生成非空训练 label。
- 命令：`qdc plan-backfill --dataset daily_bar --source-id akshare --start 2023-01-01 --end 2024-12-31 --symbols SH600000,SZ000001 --batch-size 2 --chunk-days 800` 成功，新增 1 个历史任务。
- 命令：`qdc run-backfill --dataset daily_bar --limit-tasks 3` 成功；两个旧 pending 样例任务返回 0 行，历史任务写入 968 行。
- 命令：`qdc quality --dataset daily_bar --start 2023-01-01 --end 2024-12-31` 成功，检查 968 行，0 issue。
- 命令：`qdc export-qlib --start 2023-01-01 --end 2024-12-31 --provider-uri data/quant_data_center/qlib/cn_data --market-name qdc_smoke` 成功，导出 2 个标的、484 个交易日、27 个 Qlib 文件，包含 `instruments/qdc_smoke.txt`。
- 命令：`qdc verify-qlib --start 2023-01-01 --end 2024-12-31 --instruments SH600000,SZ000001 --provider-uri data/quant_data_center/qlib/cn_data` 成功，返回 968 条 feature 行，`issues=[]`。
- 命令：本地 Qlib `Alpha158(instruments="all", start_time="2023-01-01", end_time="2024-12-31")` 成功，`handler.fetch()` 返回 `(968, 159)`，非空 label 为 964。
- 下一步：完整 baseline 需要完整 universe、指数 benchmark 和明确 train/valid/test 切分。

## Qlib LightGBM Alpha158 qrun smoke

- 配置：新增 `config/qlib/workflow_config_lightgbm_alpha158_qdc_smoke.yaml`，使用 QDC provider、`market=qdc_smoke`、`Alpha158` handler、`LGBModel`，只保留 `SignalRecord` 和 `SigAnaRecord`，不做需要指数 benchmark 的组合回测。
- 命令：`qrun config/qlib/workflow_config_lightgbm_alpha158_qdc_smoke.yaml` 成功。
- 结果：Qlib 完成数据加载、DropnaLabel、CSZScoreNorm、LightGBM 训练、`pred.pkl` artifact 保存和信号分析记录。
- 注意：当前只有 2 个标的，训练输出的 IC/Rank IC 为 `nan`，该 smoke 只证明 qrun 链路可跑，不代表策略有效性。
- 本地产物：qrun 写入 `mlruns/`，已加入 `.gitignore`。
- 下一步：扩大到完整 universe 后，补指数 benchmark / PortAnaRecord 并形成正式 baseline。

## 通用 Agent 规则接入

- 目标：把跨项目通用开发规范和自动提交收尾流程接入当前项目，并沉淀为可复用 GitHub 仓库。
- 外部仓库：新增并推送 `YYGe01/agent-dev-rules`，当前提交 `5c8887c`，包含 Python CLI、Codex plugin manifest、`finish-task-with-commit` skill、中文 AGENTS/Claude/Cursor/Git hook 模板和测试。
- 当前项目：运行 `agent-rules init --repo <current repo>`，写入或更新 `AGENTS.md`、`CLAUDE.md`、`.cursor/rules/git-workflow.mdc`、`.githooks/commit-msg`、`.agent-rules.yaml`、`.pre-commit-config.yaml` 和 `docs/智能体工程规范.md`。
- 规则口径：除自动提交收尾外，新增中文通用工程规范，包括小步改动、模块解耦、文件/函数复杂度软阈值、测试同行、契约同步、错误处理、安全边界和最小惊扰。
- 验证：`agent-rules doctor --repo <current repo>` 通过。
- 验证：`git diff --check -- AGENTS.md CLAUDE.md .cursor/rules/git-workflow.mdc .agent-rules.yaml .githooks/commit-msg .pre-commit-config.yaml docs/智能体工程规范.md` 通过。
- 提交：当前项目已单独提交规则接入，commit `7d6ffd2`，未带入 QDC 代码迁移改动。

## 文档入口清理

- 目标：在不触碰 QDC 代码主线的前提下，把迁移前的长文档从当前主线删除或合并到新的 QDC 入口文档。
- 保留入口：当前长期实施状态统一收敛到 `docs/迁移实施计划.md`，智能体工作摘要收敛到 `docs/工作日志/2026年05月11日.md`。
- 删除范围：迁移前采集湖架构、预采集需求、数据流、实时采集、前端控制台、AkShare 风险评估和事件研究文档不再作为当前主线文档保留。
- 引用检查：`README.md`、`AGENTS.md`、`.cursor/rules/` 和现有 `docs/` 已不再引用迁移前文档路径。
- 提交：并发 QDC 迁移提交 `98790cd` 已包含旧文档删除和新 QDC 文档入口收敛。
- 收尾验证：`agent-rules doctor --repo <current repo>`、`conda run -n ai-trader python -m quant_data_center.cli validate-config`、`conda run -n ai-trader pytest`、`conda run -n ai-trader python -m ruff check .` 均通过。

## 非结构化事件因子规则升级和 LLM 接口预留

- 决策：当前全量公告/新闻因子继续默认使用规则引擎，LLM 只作为单条 `qdc classify-text-event --provider llm` smoke 接口，不参与 `qdc build-factors` 批量构建。
- 新增：`src/quant_data_center/factor_engine/text_events.py`，统一规则和 LLM 的事件分类返回契约，输出 `event_types`、`sentiment_score`、`importance_score`、`weighted_sentiment`、`matched_keywords` 和 `evidence`。
- 规则：扩展标题级事件识别，覆盖增长、风险、融资、经营、合同、回购、股东增减持、控制权变更、监管、诉讼、业绩、分红、质押和担保。
- 因子：`daily_news_factor` 新增 `news_weighted_sentiment_sum`、`news_importance_sum`、`news_contract_count`、`news_buyback_count`、`news_shareholder_change_count`、`news_regulatory_count`、`news_litigation_count`、`news_performance_count`。
- 因子：`daily_announcement_factor` 新增 `announcement_sentiment_mean`、正负面计数、`announcement_growth_count`、加权情绪/重要性、合同、回购、股东变化、监管、诉讼和业绩计数。
- 导出：`qdc export-qlib`、`sync-parquet` 和 `QdcAlpha158WithExternal` 已同步新增外部事件因子字段。
- LLM：`qdc classify-text-event --provider llm` 通过 LiteLLM 读取 DeepSeek 配置；凭据不写入配置、文档或测试。
- 验证：`conda run -n ai-trader pytest tests/test_qdc_storage.py::test_qdc_build_factors_aligns_text_titles_and_labels_events tests/test_qdc_storage.py::test_qdc_classify_text_event_rule_and_mock_litellm tests/test_qdc_storage.py::test_qdc_export_qlib_writes_day_provider_files -q` 通过，3 passed。
- 验证：`conda run -n ai-trader python -m ruff check src/quant_data_center/factor_engine src/quant_data_center/storage/schema.py src/quant_data_center/storage/silver.py src/quant_data_center/exports/qlib.py src/quant_data_center/storage/parquet.py src/quant_data_center/qlib_ext/handlers.py src/quant_data_center/cli.py src/quant_data_center/factors.py tests/test_qdc_storage.py` 通过。
- 验证：`conda run -n ai-trader python -m quant_data_center.cli classify-text-event --provider rule --document-type announcement --title '公司收到交易所监管问询函'` 通过，返回 `regulatory`。
- 验证：`conda run -n ai-trader python -m quant_data_center.cli validate-config` 通过，`status=ok`。
- 验证：`conda run -n ai-trader pytest -q` 通过，30 passed。
- 验证：`conda run -n ai-trader python -m ruff check .` 通过。
- 验证：此前提供 DeepSeek key 和模型配置后，`qdc classify-text-event --provider llm --document-type announcement --title '公司拟以自有资金回购股份'` 通过，返回 `buyback`。

## LLM 配置落地 YAML

- 决策：模型切换不再依赖运行时 `LITELLM_MODEL`；`llm.text_event` 已加入 `config/quant_data_center.yaml`，配置 `provider`、`model`、`api_key_file`、`api_key_env`、`temperature` 和 `max_tokens`。
- 安全边界：真实 API key 不写入仓库配置；默认 `api_key_file` 指向 gitignored 的 `data/quant_data_center/secrets/deepseek_api_key`，LiteLLM 调用优先读该文件，缺失时再读配置指定的 env var。
- CLI：`qdc classify-text-event` 不传 `--provider` 时读取 `llm.text_event.provider`；`--provider` 只作为临时覆盖项。
- 验证：`conda run -n ai-trader pytest tests/test_qdc_storage.py::test_qdc_classify_text_event_rule_and_mock_litellm -q` 通过，1 passed；该测试覆盖不传 `--provider` 时从 YAML 读取 rule/llm provider，并通过配置的 `api_key_file` 注入 mocked LiteLLM。
- 验证：`conda run -n ai-trader python -m ruff check src/quant_data_center/settings.py src/quant_data_center/factor_engine/text_events.py src/quant_data_center/cli.py tests/test_qdc_storage.py` 通过。
- 验证：`conda run -n ai-trader python -m quant_data_center.cli validate-config` 通过，`status=ok`。
- 验证：`conda run -n ai-trader python -m quant_data_center.cli classify-text-event --document-type announcement --title '公司收到交易所监管问询函'` 通过，默认从 YAML 读取 `provider=rule` 并返回 `regulatory`。
- 验证：`conda run -n ai-trader pytest -q` 通过，30 passed。
- 验证：`conda run -n ai-trader python -m ruff check .` 通过。

## QDC 数据流阅读指南

- 新增 `docs/数据流阅读指南.md`，按数据流解释当前项目从 AkShare、回补任务、raw、bronze、silver、factor_engine、gold Parquet 到 Qlib provider 的完整链路。
- 文档面向第一次阅读项目的人，包含每一步输入输出、表结构样例、JSON/Parquet/Qlib provider 样例、新闻情绪打分例子和前端监控建议读取的控制表。
- README 已增加数据流指南和轻量控制台入口。
- 文档级验证：`git diff --check`。

## 文档中文命名

- 目标：项目文档内容使用中文，`docs/` 下文档文件和目录统一使用中文命名。
- 当前文档入口：`docs/数据流阅读指南.md`。
- 当前实施计划：`docs/迁移实施计划.md`。
- 当前工程规范：`docs/智能体工程规范.md`。
- 当前工作日志目录：`docs/工作日志/`，日期日志命名为 `YYYY年MM月DD日.md`。
- 约定：`README.md`、`AGENTS.md`、`CLAUDE.md` 保留生态约定文件名，正文和引用路径保持中文。
- 验证：旧英文文档路径引用检查通过；`git diff --check -- README.md AGENTS.md .cursor/rules/000-project-agent.mdc docs` 通过。

## 本地只读控制台和中文文档合并

- 合并并发改动：保留 `qdc console --host 127.0.0.1 --port 8765` 入口、本地只读 Web 控制台、控制台测试，以及 `docs/` 中文命名要求。
- 控制台范围：读取 DuckDB 中的总览、回补任务、任务水位、质量问题、source object、silver 预览和 Qlib 导出记录；不触发采集，不写库。
- 回补辅助：新增 `recover-running` 恢复陈旧 running task；新增 `split-backfill` 拆分大 symbol 批次，原任务标记为 `superseded`，只允许拆分 pending/failed 任务，控制台进度聚合排除 superseded 任务。
- 数据流指南已补充 `qdc console`、`recover-running` 和 `split-backfill` 使用方式；控制台页面标题改为 `QDC 控制台`。
- 验证：`conda run -n ai-trader python -m quant_data_center.cli validate-config` 通过，`status=ok`。
- 验证：`conda run -n ai-trader pytest` 通过，29 passed。
- 验证：`conda run -n ai-trader python -m ruff check .` 通过。
- 验证：旧英文文档路径引用检查和 `git diff --check` 均通过。

## csi300 daily_bar 小批次回补和监控改进

- 目标：避免长时间单任务静默运行，把剩余 csi300 `daily_bar` 历史回补拆成可观察的小批次，并通过 console/API 反馈进度。
- 控制面修复：`recover-running --dataset daily_bar --older-than-minutes 15` 恢复 1 条中断遗留的 stale running 任务。
- 批次拆分：两个 50-symbol 大任务分别用 `split-backfill --batch-size 10` 拆为 10 个 pending 子任务，原任务标记为 `superseded`。
- 前端改进：总览页新增采集进度卡片，展示 success/pending/running/failed/stale、完成率和 blocked 状态；前端自动 15 秒刷新；DuckDB 写锁期间保留上次快照并提示“正在写入”。
- 真实回补：按 `qdc run-backfill --dataset daily_bar --limit-tasks 1` 串行跑完 10 个 10-symbol 子任务；期间每批反馈 row_count 和进度。
- 结果：`daily_bar/csi300` 进度从 11/13 active task 变为 21/21 active task，最终 `pending=0`、`running=0`、`blocked=0`、完成率 100%。
- 数据量：`qdc db-info` 显示 `qdc_silver.daily_bar=143858`。
- 质量：`qdc quality --dataset daily_bar --start 2023-01-01 --end 2024-12-31` 检查 143858 行，0 issue。
- Parquet：`qdc sync-parquet --layer all` 写出 silver/gold 10 个对象，`gold/daily_research=143858` 行。
- Qlib：`qdc export-qlib --start 2023-01-01 --end 2024-12-31 --provider-uri data/quant_data_center/qlib/cn_data --market-name csi300` 成功，299 个标的、484 个交易日、6282 个文件。
- Qlib 验证：`qdc verify-qlib --start 2023-01-01 --end 2024-12-31 --instruments SH600000,SZ300750,SH688981 --provider-uri data/quant_data_center/qlib/cn_data` 成功，1452 行 feature，`issues=[]`。
- 导出优化：`QlibExporter` 的 `source_object` 登记改为批量插入；`export-qlib` CLI 输出改为摘要，避免打印数千个 `object_id`。

## csi300 约束数据补齐和每日联动验证

- 目标：补齐 csi300 同区间 `adj_factor`、`price_limit` 和完整 `trade_calendar`，并验证每日联动采集可以把已规划任务全部清空。
- 历史回补：`adj_factor/csi300` 和 `price_limit/csi300` 均完成 31/31 active tasks，`pending=0`、`running=0`、`blocked=0`、完成率 100%。
- 交易日历：新增 `trade_calendar` 历史任务覆盖 `2023-01-01` 到 `2024-12-31`，写入 484 个开市日。
- 质量：`qdc quality --dataset adj_factor --start 2023-01-01 --end 2024-12-31` 检查 143858 行，0 issue；`price_limit` 检查 143853 行，0 issue；`trade_calendar` 检查 484 行，0 issue。
- 每日联动：`qdc daily --date 2024-12-31 --universe csi300 --source-id akshare --batch-size 50 --limit-tasks 4` 规划 27 个日任务，随后用 `qdc run-backfill --dataset <dataset> --limit-tasks 1` 串行清空剩余 pending。
- 每日结果：`daily_bar` 当日批次成功，`trade_status` 写入 704 条当日停牌状态，`announcement` 写入 4117 条当日公告，`news` 因 AkShare `stock_news_em` 当前返回空结果，任务成功但写入 0 行。
- 因子重建：`qdc build-factors --factor-set announcement_v1 --start 2023-01-01 --end 2024-12-31` 生成 1877 行公告日频因子；`news_v1` 生成 0 行。
- 最终 DB：`qdc db-info` 显示 `daily_bar=143859`、`adj_factor=143858`、`price_limit=143853`、`trade_calendar=484`、`trade_status=1554`、`announcement=4969`、`daily_announcement_factor=1877`、`news=0`、`daily_news_factor=0`、`quality_issue=0`。
- 最终质量：`qdc quality --start 2023-01-01 --end 2024-12-31` 检查 446269 行，0 issue。
- Parquet：`qdc sync-parquet --layer all` 写出 10 个对象，`gold/daily_research=143859` 行。
- Qlib：`qdc export-qlib --start 2023-01-01 --end 2024-12-31 --provider-uri data/quant_data_center/qlib/cn_data --market-name csi300` 成功，299 个标的、484 个交易日、12262 个文件。
- Qlib 验证：`qdc verify-qlib --start 2023-01-01 --end 2024-12-31 --instruments SH600000,SZ300750,SH688981 --provider-uri data/quant_data_center/qlib/cn_data` 成功，1452 行 feature，`issues=[]`。
- 运行注意：`qdc console` 会持有 DuckDB 连接；真实回补、质量写入和同步导出前先停 console，完成后再启动，避免文件锁冲突。
- 下一步：形成 csi300 Alpha158 baseline qrun 配置和指数 benchmark；同时评估替代新闻源，解决 AkShare 新闻长期为空的问题。

## 控制台中文术语改进

- 目标：让使用者从前端直接看懂采集流程和状态，不需要理解所有英文控制字段。
- 改进：`qdc console` 的数据集、任务状态、文件层级、股票池、数据来源、表格列名和筛选项改为中文优先展示，并在括号中保留英文代码名。
- 示例：`daily_bar` 显示为 `日线行情 (daily_bar)`，`pending` 显示为 `待执行 (pending)`，`source_id` 显示为 `数据来源 (source_id)`。
- 保留：命令参数、数据库字段和 Qlib 固有字段仍保留英文代码名，避免和 CLI、DuckDB 表字段、Qlib 表达式脱节。

## 数据流阅读指南术语中文化

- 目标：让第一次阅读 `docs/数据流阅读指南.md` 的人先看到中文概念，再对应到代码和命令里的英文标识。
- 更新：新增“术语对照和阅读约定”，覆盖数据集、数据来源、股票池、标的、任务、水位、raw/bronze/silver/gold、因子、handler、baseline 和 smoke。
- 更新：正文高频术语改为中文优先表达，例如原始留档层、上游快照层、统一研究层、研究宽表层、Qlib 数据目录、处理器和冒烟验证。
- 更新：`README.md` 的指南入口描述同步改为中文术语，命令、字段名、表名和路径仍保持英文。

## 数据集和字段术语补译

- 目标：补清 `adj_factor` 等代码名的中文含义，降低第一次阅读 QDC 文档的门槛。
- 更新：`docs/数据流阅读指南.md` 新增常见数据集名称和常见字段名称对照，覆盖 `stock_basic`、`trade_calendar`、`daily_bar`、`adj_factor`、`price_limit`、`trade_status`、`announcement`、`news`、字段状态、Qlib 特征和导出目录字段。
- 说明：`adj_factor` 明确解释为复权因子，用于处理分红、送转、拆股、配股等权益变动造成的价格断点，Qlib 导出时对应 `factor.day.bin`。
- 更新：`README.md` 的已支持数据集列表改为中文名 + 英文代码名，并补译 `provider`、`smoke`、`handler`、`universe`、`train/valid/test` 等入口说明。

## 新闻替代源和 csi300 baseline

- 调研：Tushare `news` 接口适合历史快讯，覆盖 6 年以上历史，但需要单独权限；聚宽/企查查/Wind 类方案适合生产级历史舆情，但需要账号或付费授权；AkShare 免费公开源更适合近实时标题级补位。
- 实现：`collect_news` 保留东方财富 `stock_news_em` 主路径；当主路径为空时，回退到 AkShare `stock_info_global_cls`、`stock_info_global_em`、`stock_info_global_ths`、`stock_info_global_sina` 和 `stock_info_global_futu` 全局资讯流。
- 映射：全局资讯不会无脑写给所有股票，只有标题或正文命中股票代码、内部标的代码或证券简称时，才写入对应 `instrument`。
- 测试：新增 `test_qdc_news_falls_back_to_global_stream_and_maps_instrument`，覆盖个股新闻为空、全局资讯命中 `宁德时代(300750.SZ)` 后写入 `SZ300750`。
- 验证：`conda run -n ai-trader pytest tests/test_qdc_storage.py::test_qdc_news_falls_back_to_global_stream_and_maps_instrument tests/test_qdc_storage.py::test_qdc_news_provider_error_is_recorded_without_failing_task tests/test_qdc_storage.py::test_qdc_run_backfill_announcement_news_and_build_factors_with_fake_akshare -q` 通过，3 passed。
- 验证：`conda run -n ai-trader python -m ruff check src/quant_data_center/collectors/akshare.py tests/test_qdc_storage.py` 通过。
- 真实 smoke：`qdc plan-backfill --dataset news --source-id akshare --start 2026-05-11 --end 2026-05-11 --symbols SZ300750,SH600000 --batch-size 2 --chunk-days 1` 新增 1 个任务；`qdc run-backfill --dataset news --limit-tasks 1` 成功，写入 6 条新闻。
- 质量：`qdc quality --dataset news --start 2026-05-11 --end 2026-05-11` 检查 6 行，0 issue。
- 因子：`qdc build-factors --factor-set news_v1 --start 2026-05-11 --end 2026-05-11` 成功，生成 2 行 `daily_news_factor`。
- baseline：新增 `config/qlib/workflow_config_lightgbm_alpha158_qdc_csi300.yaml`，`qrun` 成功，测试段 `IC=0.02999534584502278`、`Rank IC=0.02687899070796934`。
- external baseline：新增 `config/qlib/workflow_config_lightgbm_alpha158_qdc_csi300_external.yaml`，`qrun` 成功，指标与基础 baseline 一致；当前历史新闻因子为空，所以这次只验证外部字段链路不破坏训练。
- 数据后验：`qdc sync-parquet --layer all` 写出 12 个对象，`news=6`、`daily_news_factor=2`；`qdc quality --start 2023-01-01 --end 2026-05-11` 检查 446277 行，0 issue。
- 全量验证：`conda run -n ai-trader python -m quant_data_center.cli validate-config` 通过；`conda run -n ai-trader pytest -q` 通过，31 passed；`conda run -n ai-trader python -m ruff check .` 通过；`node --check src/quant_data_center/console_static/app.js` 和 `git diff --check` 通过。
- 后续：如要评估新闻 alpha，必须接入 Tushare/JQData/Wind 等授权历史新闻源，补齐 2023-2024 新闻长样本。
