# 2026-04-28

## PitLake Console 阶段 2 继续实现

- 状态：2026-04-27 已完成阶段 1 MVP；本次继续补齐阶段 2 的只读 drilldown，并补上阶段 1 里尚未落地的 source x date 状态矩阵。
- 修改：`src/pitlake/ui/console_data.py` 新增 dataset items/coverage/quality/reconciliation、symbol universe、symbol detail、source matrix、manifest list/detail 和扩展搜索 payload。
- 修改：`src/pitlake/ui/server.py` 新增 `/api/datasets/{logical_dataset}/items`、`coverage`、`quality`、`reconciliation`、`/api/symbols`、`/api/symbols/{symbol}`、`/api/manifests` 和 `/api/manifests/{manifest_id}`。
- 修改：静态前端新增“股票”“对账”“Manifest”入口；首页新增 source x date 状态矩阵；dataset 详情新增 Explore、Coverage 和 Reconciliation；股票详情显示 registry sample symbols 口径下的 present/missing 覆盖和 raw 证据链。
- 文档：更新 `README.md`、`docs/v0_runtime_decisions_zh.md` 和 `docs/data_lake_frontend_console_design_zh.md`，记录当前控制台能力和股票缺失检查口径。
- 口径：股票缺失检查只基于 `source_registry.yaml` 的 registry sample symbols 和当天已观测 item；没有 security master / stock universe 前，不声明全市场缺失。
- 测试：扩展 `tests/test_ui_console_data.py`，覆盖 source matrix、symbol coverage、missing symbol、manifest detail 和 raw 搜索。
- 验证：`pytest tests/test_ui_console_data.py -q --basetemp .codex_tmp_ui_pytest2 -p no:cacheprovider` 通过，2 passed；`ruff check src/pitlake/ui tests/test_ui_console_data.py` 通过；`node --check src\pitlake\ui\static\app.js` 通过。
