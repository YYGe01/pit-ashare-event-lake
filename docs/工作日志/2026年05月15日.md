# 2026年05月15日

## 2026-05-15 当日真实采集

- 按每日采集 skill 执行 `2026-05-15` 分步链路：`crawl-daily -> build-factors -> sync-parquet -> quality -> daily-health`。
- 首轮 `qdc crawl-daily --date 2026-05-15 --watch` 中，`cninfo_announcement` source timeout，`sse_announcement` 请求超时；新闻、研报、公开舆情和互动问答完成。
- `cninfo_announcement` 使用 `--source-timeout-seconds 900 --force` 单源补跑成功，写入 `provider_record_count=2010`、`document_count=2010`。
- `sse_announcement` 在 `page_size=30/100` 下连续出现 `RemoteDisconnected`；手工验证 `page_size=100` 时第 4 页断开，改用 `--page-size 500 --request-timeout-seconds 120 --source-timeout-seconds 900 --force` 后完整扫描成功，写入 `provider_record_count=748`、`document_count=748`。
- 其他默认源结果：`sina_finance_news` 补跑后仍为 `0` 行，manifest 显示 `date_scan_complete=true` 且 stop reason 为 `empty_page`；`eastmoney_roll_news` 写入 `provider_record_count=3720`、`document_count=1684`；`eastmoney_research_report` 写入 `23` 行；`eastmoney_public_sentiment` 写入 `5182` 行；`cninfo_investor_interaction` 写入 `60` 行。
- `qdc build-factors --factor-set all --start 2026-05-15 --end 2026-05-15` 成功生成 `7358` 行：`news_v1=1082`、`announcement_v1=1041`、`research_report_v1=23`、`investor_interaction_v1=30`、`public_sentiment_v1=5182`。
- `qdc sync-parquet --layer all` 成功，写出 `11` 个 silver Parquet 对象；`qdc quality --start 2026-05-15 --end 2026-05-15` 检查 `22580` 行，`issue_count=0`。
- `qdc daily-health --date 2026-05-15 --format markdown` 最终状态为 `warning`，`Errors=0`、`Warnings=2`。残余 warning 为 `sina_finance_news` 当日空结果，以及 `eastmoney_roll_news mapping_rate=55.17%`；东方财富新闻 manifest 显示 `stock_basic` 字典数 `5515`、解析失败率 `0.00%`，当前不做低置信映射放宽。

## 2026-05-15 滚动新闻增量游标优化

- 针对“同一天多次采集不能重复扫大量页面”的要求，新增 `qdc_meta.crawl_cursor` 控制表，按 `source_id + dataset + cursor_scope` 保存滚动源最近目标日 `source_record_id`、最新记录时间和扫描元数据。
- `scan_rolling_date_window` 新增游标停止能力：同一目标日重复扫描时，如果页面中出现已采集过的目标日记录，停止在该页，只返回游标之前的新记录，并在 manifest 写入 `date_scan_stop_reason=cursor_seen`、`incremental_cursor_stop_page` 和 `incremental_cursor_stop_key`。
- `eastmoney_roll_news` / `sina_finance_news` 接入目标日游标；只有上次扫描 `date_scan_complete=true` 时才启用 cursor 停止，避免上次未扫完整时漏掉更深页面。
- 新增测试覆盖数据库游标 upsert、滚动扫描命中 cursor 后只取新记录、东方财富滚动新闻第二次同日采集只请求第 1 页并更新 cursor。
- 待用 `2026-05-15` 真实采集验证：先跑完整 `crawl-daily` 建立游标，再对滚动新闻源同日 `--force` 重跑，检查 manifest 是否出现 `cursor_seen` 并确认请求页数下降。
- 2026-05-15 真实全量采集时东财滚动新闻列表页出现一次 10 秒读超时。为避免过短单请求超时导致整源失败后重复重跑，将 `EASTMONEY_LIST_REQUEST_TIMEOUT_SECONDS` 从 10 秒调到 30 秒；不增加自动重试次数，优先控制接口请求量。
- 公开舆情源 `akshare.stock_comment_em` 在真实补跑中连续触发远端断开；改为直接调用同一个东财 datacenter API，补 headers 和 request timeout，且复用第一页结果，避免 akshare 原实现第一页重复请求。同步修复公开舆情 manifest 映射指标：provider key 和 mapped key 统一为 `date|instrument`，避免 silver/factor 正常但 `mapping_rate=0%` 的误报，并把该误报处理规则写入项目 skill。
- 修复 `daily-health` 在同日 `--force` 重跑后的 manifest 统计：非增量源只使用最新 full manifest，滚动增量源使用最新 full manifest 加后续 incremental manifest，避免重复 full manifest 把 provider 和 mapping 指标翻倍。同步让独立 `qdc sync-parquet` 记录 `job_type=sync_parquet` 的 job_run，避免已同步但 health 报 stage missing。

## 2026-05-14 采集补齐和预览验收

- 停止旧控制台进程后补采 `2026-05-14` 东方财富滚动新闻，`eastmoney_roll_news` 成功写入 `document_count=1618`，覆盖 `946` 个标的。
- 补采 `2026-05-14` 东方财富研报 metadata，`eastmoney_research_report` 成功写入 `33` 行，覆盖 `31` 个标的。
- 补采 `2026-05-14` 东方财富公开舆情 metadata，`eastmoney_public_sentiment` 成功写入 `5182` 行，覆盖 `5182` 个标的。
- 互动问答源 `cninfo_investor_interaction` 需要按公司逐个访问互动易接口，不存在可用的全市场按日期查询接口；用户确认全量速度太慢后暂停本轮全量采集，只保留当前已采集结果。本轮暂停前未完成整源落库，因此 `2026-05-14` 互动问答当前为 `0` 行。
- 为互动问答 crawler 增加单标的问答页失败容错：某个公司问答页请求失败时记录 `question_failures` 并继续后续标的，避免单点网络错误拖垮整源。
- 重建 `2026-05-14` 因子：`daily_news_factor=946`、`daily_announcement_factor=843`、`daily_research_report_factor=31`、`daily_investor_interaction_factor=0`、`daily_public_sentiment_factor=5182`。
- `qdc sync-parquet --layer all` 成功写出 `stock_basic`、`announcement`、`news`、`research_report`、`public_sentiment` 和对应日频因子 Parquet。
- `qdc quality --start 2026-05-14 --end 2026-05-14` 检查 `21670` 行，`0` 个 issue。
- 控制台已启动在 `http://127.0.0.1:8765/`；`/api/daily-wide-preview?date=2026-05-14&mode=factor&limit=6000` 返回 `row_count=5520`、`hidden_count=0`，列包含 `news_count`、`announcement_count`、`research_report_count`、`investor_interaction_count` 和 `public_sentiment_count`。

### 采集耗时统计（不含互动问答）

统计口径：

```text
2026-05-15 手动补跑的 eastmoney_roll_news / eastmoney_research_report / eastmoney_public_sentiment 使用终端实测 wall time。
2026-05-14 原批次的 cninfo_announcement / sse_announcement / sina_finance_news 没有单源 wall time 日志，使用 qdc_meta.crawl_task.created_at -> updated_at 作为近似耗时。
cninfo_investor_interaction 已按用户要求暂停，不纳入本表。
```

| source_id | 数据集 | 2026-05-14 入库行数 | 覆盖标的数 | 本轮耗时 |
| --- | --- | ---: | ---: | ---: |
| `cninfo_announcement` | `announcement` | 1518 | 843 | 230 秒（约 3 分 50 秒） |
| `sse_announcement` | `announcement` | 802 | 453 | 406 秒（约 6 分 46 秒） |
| `sina_finance_news` | `news` | 0 | 0 | 587 秒（约 9 分 47 秒） |
| `eastmoney_roll_news` | `news` | 1618 | 946 | 167.5 秒（约 2 分 48 秒） |
| `eastmoney_research_report` | `research_report` | 33 | 31 | 36.7 秒 |
| `eastmoney_public_sentiment` | `public_sentiment` | 5182 | 5182 | 143.4 秒（约 2 分 23 秒） |

补充说明：`eastmoney_roll_news` 的控制表任务累计 `attempt_count=5`，早先失败尝试导致 `crawl_task.created_at -> updated_at` 跨天，不适合用于成功补跑耗时；上表使用成功补跑命令的终端 wall time。

## 互动问答采集优化实现

- 用户要求将不用代理的互动问答采集策略放入单独 Markdown 文档，同时实现优化并用小样本真实运行估算全市场耗时。
- 新增 `docs/互动问答采集优化方案.md`，单独记录 orgId/secid 缓存、源内标的并发、全市场保护阈值、日期早停、失败容错、推荐命令和不用代理的耗时评估方法。
- `cninfo_investor_interaction` 新增本地 orgId 缓存：`data/quant_data_center/cache/cninfo_investor_interaction_orgs.json`；缓存命中时跳过 `queryKeyboardInfo`。
- `crawl-daily` / `crawl-run` 新增 `--instrument-parallelism` 和 `--instrument-limit`；`--instrument-limit 0` 表示显式允许使用全部 active instruments，保留默认前 50 的误触发保护。
- 互动问答按标的内部并发执行，单个标的 question 请求失败不再中断整批；manifest 和任务结果会记录 request_count、org_cache 命中/更新数、org/question 失败数。
- 已增加单测覆盖 question 单标的失败容错、orgId 缓存命中和遇到早于目标日数据后的翻页早停。
- 不用代理真实耗时样本：本机 active instruments 为 5515；20 个混合标的冷启动 35.39 秒，缓存命中后 21.99-25.07 秒；50 个混合标的部分冷启动 65.56 秒，缓存命中后 44.60 秒；`instrument_parallelism=12` 为 44.79 秒，较 8 无明显改善。
- 基于 50 个缓存命中样本估算，全市场日更约 82 分钟；首次冷启动建缓存约 2.0-2.7 小时。该估算使用 `page_size=50`、`max_pages=1`，若放宽翻页会更慢。
- 后续按严格覆盖优先继续优化：互动问答缓存扩展为 orgId/secid + 探测状态，记录 org_status、question_status、last_checked_date、last_target_date、latest/oldest provider publish date、target_row_count、provider_record_count、page_count、consecutive_no_data_days、latency 和 last_error。
- 互动问答源新增每线程 `requests.Session()` 连接复用；默认每个标的最多扫 2 页，显式 `--max-pages` 可覆盖。任务结果会输出 `effective_max_pages`、`instrument_status_counts` 和 `target_date_instrument_count`，用于估算覆盖和后续是否需要 fast 模式。
- 二次优化后不用代理样本：50 个缓存命中标的 page 1 探测 31.73 秒，默认 2 页 39.46 秒；100 个缓存命中标的 page 1 探测 57.47 秒，默认 2 页 68.08 秒。按 5515 个 active instruments 估算，严格覆盖日更约 53 分钟（page 1）到 63 分钟（默认 2 页）。

## 互动问答 cold-weekly / adaptive 前向降频策略

- 用户确认“一周”只是示例，要求按最佳实践实现冷标的降频采集；本轮新增 `--interaction-schedule cold-weekly`，`adaptive` 为同义别名。
- 默认策略参数：连续 `14` 次无目标日数据进入 cold；cold 标的每 `7` 个自然日复查；复查接受最近 `14` 个自然日窗口；`missing_org` / unsupported 标的每 `30` 个自然日复查。
- `crawl-daily` / `crawl-run` 新增 `--interaction-cold-no-data-days`、`--interaction-cold-check-interval-days`、`--interaction-cold-lookback-days` 和 `--interaction-unsupported-check-interval-days`；`daily-pipeline --crawl-documents` 对应新增 `--crawl-interaction-*` 参数。
- cold 复查命中窗口内旧日期问答时，记录按真实 `publish_date` 入库，并保留本次真实 `observed_at` / `collect_time`；这属于前向延迟覆盖，不是历史回采。
- `daily-pipeline` 在文档采集返回 `affected_publish_dates` 时，会把本轮因子重算起点提前到最早受影响日期，避免 cold 窗口补到旧 `publish_date` 后只重算运行日。
- manifest / 命令结果新增 `interaction_schedule`、`schedule_decision_counts`、`selected_instrument_count`、`skipped_instrument_count`、`accepted_start_date`、`accepted_end_date`、`affected_publish_dates` 和 `accepted_window_instrument_count`。
- 已补单测覆盖 cold 未到期跳过、cold 到期窗口采集、旧 `publish_date` 入库、缓存状态更新和 daily-pipeline 因子重算起点选择。
- 不用代理真实小样本：本地 `stock_basic` 前 100 个 active instruments 在冷启动建缓存时耗时 90.10 秒；同批缓存命中并临时设置 `cold_no_data_days=1` 模拟成熟 cold 状态时，`page_size=50,max_pages=1` 请求 38 个、跳过 62 个，耗时 80.01 秒；`page_size=10,max_pages=1` 降到 46.48 秒但明显截断记录；`page_size=30,默认2页` 较完整口径耗时 106.63 秒。
- 结论：cold 调度能减少无效请求，但高互动 hot 标的的 payload 和翻页仍会主导耗时；后续估时必须同时看 `selected/skipped/request_count/provider_record_count`，不能只按请求数外推。
- 用户进一步确认默认就使用冷标策略。本轮将 `cninfo_investor_interaction` 默认调度从 `strict` 改为 `cold-weekly`；`config/quant_data_center.yaml` 的 `daily_pipeline` 段已配置 `crawl_instrument_parallelism=8`、`crawl_instrument_limit=0`、`crawl_interaction_schedule=cold-weekly` 和 cold 参数，并把文档采集 source timeout 调整为 `7200` 秒。
- 用户要求完整采集命令不要堆长参数。本轮把 `daily-pipeline` 的 `date`、`date_offset_days`、`symbols`、`plan_only`、`control_only`、`watch` 等剩余 CLI 默认项也纳入 `daily_pipeline` 配置模型，并补齐 `config/quant_data_center.yaml` 中所有 daily-pipeline 参数。新增 `config/README.md` 逐项说明配置作用；当前 `date_offset_days=-1`，日常采集昨日数据可直接运行 `qdc daily-pipeline`，补跑固定日期再传 `--date YYYY-MM-DD`。

## 每日采集健康检查和项目 skill

- 用户要求给出并实现每日采集质量口径，后续可直接 @skill 完成采集任务。
- 新增 `qdc daily-health`，按目标日期聚合 `crawl_task`、`crawl_run`、raw document manifest、silver 行数、daily factor 行数、`qdc quality` issue 和近 20 日历史中位数，输出 `ok / warning / error`。
- `daily-health` 默认将“源返回 provider 记录但 silver 为空”“解析失败率高”“映射率过低”“交易日公告/新闻/公开舆情关键组全为空”“有文档但无因子行”“qdc quality 有 error issue”等判为 error；将日期扫描不完整、稀疏源为空、低于历史基线等判为 warning。
- 新增 `.codex/skills/qdc-daily-collection/SKILL.md`，固定每日采集流程：`crawl-daily -> build-factors -> sync-parquet -> quality -> daily-health`，并规定按 `daily-health` 的 `code` 先分类，再决定重跑、观察或修复代码。
- 已补单测覆盖：`provider_record_count > 0` 但 silver 为空时 `daily-health` 报 error；`cninfo_investor_interaction` 作为稀疏源 0 行不误报。

## 每日采集进度和日志可见性

- 用户澄清默认每日采集应按 skill 的分步流程执行，不应把 `daily-pipeline` 作为主入口；`daily-pipeline` 继续作为包含历史结构化 `daily_bar`、可选文档采集、因子、Parquet、quality 和 Qlib 导出的保留一键入口。
- 默认 `crawl-daily` 源顺序调整为公告、新闻、研报、公开舆情、互动问答；最慢的 `cninfo_investor_interaction` 放在最后。
- `crawl-daily` 实际执行前会按 `CRAWL_DAILY_SOURCE_IDS` 重新排序任务，避免控制表查询按 `source_id` 字母序把互动问答提前。
- `crawl-daily` 新增 `--watch`，直接命令和控制台启动都能输出 source 级 RUNNING / OK / FAIL 日志；控制台启动时自动带 `--watch`。
- README 和项目 skill 补充本地日志建议：Codex 或自动化代跑时，用 PowerShell `Tee-Object -Append` 把输出写入 `data/quant_data_center/logs/daily-collection-<date>.log`。
