# 2026-04-27

## 政策监管官方列表 connector

- 目标：继续补免费 official/shadow source，把 `policy_regulatory_doc` 从 AkShare CCTV bootstrap 推进到官方列表源可手动对账。
- 修改：新增 `src/pitlake/connectors/policy/html_list.py` 通用 HTML 列表采集器，以及 `csrc.py`、`gov_cn.py`、`pbc.py` 三个具体 connector。
- 配置：`csrc_policy_news`、`gov_cn_policy`、`pbc_policy_news` 从 `planned` 改为 `active_shadow`，仍保持 `enabled: false`。
- 实现口径：只保存列表页 raw HTML、标题、URL、发布日期、部门和分类；不下载详情页、附件或正文。
- 测试：新增 `tests/test_policy_html_list_connectors.py`，覆盖 CSRC/gov.cn/PBC 三类页面的链接过滤、日期解析、字段标准化和 metadata 入账。
- 真实验证：`csrc_policy_news` 成功，`new_item_count=13`；`gov_cn_policy` 成功，`new_item_count=14`；`pbc_policy_news` 成功，`new_item_count=15`。

## SHFE 商品日频官方 connector

- 目标：继续补 P0 商品日频 official source，用上期所公开交易所 JSON 对 AkShare 商品样例源形成对账候选。
- 修改：新增 `src/pitlake/connectors/commodities/shfe.py`，使用 SHFE `/data/tradedata/future/dailydata/kxYYYYMMDD.dat` 接口采日交易快讯。
- 配置：`shfe_daily_commodity` 从 `planned` 改为 `active_shadow`，仍保持 `enabled: false`。
- 实现口径：保存交易所 raw JSON，并将 `o_curinstrument` 合约行标准化到 `commodity_daily`，字段包括合约、交易日、开高低收、结算价、前结算、成交量和持仓量。
- 测试：新增 `tests/test_shfe_daily_connector.py`，覆盖 SHFE JSON 字段标准化和 metadata 入账。
- 真实验证：运行 `pitlake run-source --source-id shfe_daily_commodity --end-date 20260424 --manifest-date 2026-04-27` 成功，过滤非合约行后 `new_item_count=332`、`quarantine_count=0`。
- 附带发现：Stooq 当前 CSV 下载端点要求通过官方页面/验证码获取 apikey，已将 `stooq_global_daily` 标记为 `planned_credential_required`，不绕过验证码。

## CZCE 和 GFEX 商品日频官方 connector

- 目标：继续补商品交易所 official source，覆盖郑商所和广期所。
- 修改：新增 `src/pitlake/connectors/commodities/czce.py`，使用 CZCE `/cn/DFSStaticFiles/Future/YYYY/YYYYMMDD/FutureDataDaily.txt` 静态文本文件采期货每日行情。
- 修改：新增 `src/pitlake/connectors/commodities/gfex.py`，使用 GFEX `/u/interfacesWebTiDayQuotes/loadList` JSON 接口采日行情。
- 配置：`czce_daily_commodity`、`gfex_daily_commodity` 从 `planned` 改为 `active_shadow`，仍保持 `enabled: false`；`dce_daily_commodity` 标记为 `planned_blocked_source_response`。
- 测试：新增 `tests/test_czce_daily_connector.py` 和 `tests/test_gfex_daily_connector.py`，覆盖文本/JSON 字段标准化和 metadata 入账。
- 真实验证：`czce_daily_commodity` 成功，`new_item_count=241`；`gfex_daily_commodity` 成功，`new_item_count=48`。
- 阻塞：DCE `publicweb/quotesdata/dayQuotesCh.html` 在本环境返回 HTTP 412，普通浏览器头也不放行；先不绕过，等待确认官方可访问路径。
