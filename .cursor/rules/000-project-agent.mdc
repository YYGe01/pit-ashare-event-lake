---
description: quant_data_center 项目级 agent 工作流规则。
globs: ["**/*"]
alwaysApply: true
---

# quant_data_center Agent 规则

- 使用 `AGENTS.md` 作为 Codex 和 Cursor 共用的项目级指令文件。
- 默认用中文和用户沟通，项目文档也尽量用中文；代码标识符、配置 key、dataset 名和 schema 字段保持英文。
- 继续之前的工作时，先读取 `README.md`、`docs/quant_data_center_migration_plan_zh.md` 和 `config/quant_data_center.yaml`。
- 当前主线是个人量化统一数据中心：采集、回补、清洗、日频因子加工和 Qlib 导出。
- 本仓库不做模型训练、组合回测、实盘下单或交易终端适配。
- raw/bronze 对象采用追加写入；不要覆盖历史对象。
- 运行数据放在 `data/quant_data_center/`，不要提交到 git。
- 不要把真实凭据、cookie 或 API key 写进仓库。
- 完成代码改动前，在环境可用时运行 `qdc validate-config`、`pytest` 和 `ruff check .`。

@AGENTS.md
