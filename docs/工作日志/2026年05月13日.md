# 2026年05月13日

## P0 多源并行和超时容错落地

- 结构化 `daily-pipeline` 新增 `source_ids` 配置和 `--source-ids` 覆盖参数；默认配置为 `akshare`、`eastmoney`、`sina`，其中 Eastmoney 覆盖 `daily_bar` / `adj_factor` / `price_limit` / `trade_status`，Sina 覆盖 `trade_calendar`。
- 结构化任务新增 `daily_parallelism` / `--daily-parallelism`，支持多个 backfill task 并行执行，控制台 watch 会显示并行任务数。
- 新增 `EastmoneySilverCollector`，通过 AkShare 的 Eastmoney 历史行情接口写 raw、bronze 和 silver，并用前收盘价推导 `daily_bar.pre_close` 与涨跌停价。
- 文档爬虫 `_run_crawl_tasks` 改为源级并行，新增 `crawl_parallelism` / `--parallel-sources`、`crawl_request_timeout_seconds` / `--request-timeout-seconds` 和 `crawl_source_timeout_seconds` / `--source-timeout-seconds`。
- 新增 crawler runtime deadline helper；CNINFO、SSE、Sina、Eastmoney 和 NBD 文档源在请求、翻页、PDF 下载和源间等待时都会检查 deadline。单源超时写入 `crawl_task.last_error`，同 dataset 其他成功源继续保存 raw/silver。
- 新增 `nbd_company_news` metadata-only 新闻源，并纳入默认 `crawl-daily` 新闻源；文本因子和控制台文档口径同步识别 NBD。
- `daily-pipeline --crawl-documents` 的文档步骤改为 partial-success 口径：同一 dataset 只要至少一个源成功，就继续后续因子/同步/质量/导出；只有某 dataset 候选源全部失败或仍有未跑任务时才标记 partial。
- 结构化 daily step 同步使用 partial-success 口径：同一 dataset、日期和 symbol batch 只要有一个结构化源成功，就不阻塞后续步骤；只有所有候选源都失败的 unit 才进入 `exhausted_units`。

## 文档和配置

- `config/quant_data_center.yaml` 和 `config/quant_data_center_daily_only.yaml` 已写入结构化多源、结构化并行、文档源并行和文档源超时默认值。
- `README.md` 更新每日链路说明：默认结构化多源、默认五个文档源、文档源并行超时和命令行覆盖方式。
- `docs/每日自动采集实施计划.md` 更新 P0 状态：多源并行、今日总览失败统计、发布时间归属已进入第一版落地状态。
- `docs/迁移实施计划.md` 更新每日自动采集阶段结论，去掉“文档源仍串行”的旧描述。

## 验证

- `conda run -n ai-trader qdc validate-config` 通过。
- `conda run -n ai-trader qdc --config config/quant_data_center_daily_only.yaml validate-config` 通过。
- `conda run -n ai-trader pytest -q` 通过，62 passed。
- `conda run -n ai-trader ruff check .` 通过。
- `node --check src/quant_data_center/console_static/app.js` 通过。
- `git diff --check` 通过。

## 剩余事项

- 需要全市场真实运行验证 `source_ids: [akshare, eastmoney, sina]` 的覆盖率、耗时和 DuckDB 锁等待情况。
- 结构化源级 timeout 目前主要依赖底层接口返回和任务级失败记录；如真实运行发现单个底层接口长时间阻塞，需要继续把结构化 fetch 与 persist 拆成可中断进程模型。

## 非新闻公告信息源调研沉淀

- 按用户要求不新增独立文档，把“除新闻和公告外的重要非结构化/另类信息源”沉淀到 `docs/每日自动采集实施计划.md`。
- 新增 P4 候选池，明确当前只记录调研结论，不进入 P0/P1；等固定标的每日采集和连续交易日稳定性通过后，再从候选中选择 1 到 2 类做最小可验证增量。
- 候选优先级记录为：互动问答/投资者关系、股吧雪球舆情、招聘 JD、招投标/政府采购、研报全文/评论、法律处罚文本优先；专利/商标/软著、消费者投诉/电商评价/App 评价、Web/App/搜索热度次之；遥感图像、门店客流、物流/航运/航班痕迹暂列第三批。
- 文档同步记录了合规边界：只采公开、合法、可追溯来源；不绕过登录、验证码、付费墙，不保存 cookie/token/账号密码；每次只新增一类信息源并补齐 raw/bronze/silver、控制台预览、quality、因子和 Qlib 导出边界。

## docs 第一轮清理

- 删除 `docs/量化非结构化数据每日爬虫实施方案.md`。该文件已经归档，当前执行内容已合并到 `docs/每日自动采集实施计划.md`。
- `docs/迁移实施计划.md` 删除归档文档索引，并把原第 14 到 21 节的大量 smoke 命令、baseline 结果和每日采集流水账压缩为“运行验证摘要”和“当前执行入口”。
- `docs/工作日志/说明.md` 更新文档职责：长期架构和边界写迁移实施计划，每日采集当前状态和下一步写每日自动采集实施计划。

## 固定文档冒烟复验

- 使用 `config/quant_data_center_daily_only.yaml` 复验 `2026-05-11` 固定文档冒烟标的 `SZ301421,SZ300001`。
- 结构化日频初次使用 `daily_parallelism=2` 触发 AkShare `py_mini_racer` 底层 fatal；恢复 running 任务后改为 `daily_parallelism=1` 串行运行，16/16 结构化任务全部成功。
- CNINFO 公告源成功：provider 270 条，映射入库 20 条公告，20 个 PDF 全部保存，`SZ300001=13`、`SZ301421=7`。
- NBD 新闻源失败：`https://www.nbd.com.cn/columns/1285/` 返回 SSL EOF，HTTP 回退为 502；重试后仍失败，因此本轮 `news` 和 `daily_news_factor` 为 0。
- `build-factors` 成功：`daily_announcement_factor=2`、`daily_news_factor=0`；`quality --start 2026-05-11 --end 2026-05-11` 检查 61 行，0 issue；`export-qlib` 导出 2 标的、1 日历、85 文件；`verify-qlib` 读取 2 行 feature，issues=[]。
- 前端检查发现控制台今日总览和宽表在 stock_basic/universe 为空时错误回退到配置里的 csi300 示例标的，导致固定 smoke 数据不可见；已修复为优先使用当日核心日频/文档/因子观测标的，且排除 `trade_status` 这类稀疏全市场状态表。
- 同步修复 Windows DuckDB 锁错误识别和路径分隔符测试断言；完整验证通过：`qdc validate-config`、daily-only `validate-config`、`ruff check .`、`pytest -q`。

## 新闻正文预览优化

- 用户查看 `SH601991` 新闻明细时发现前端只提示“本地仅保存列表/元数据 JSON”，无法像公告 PDF 一样看到正文内容。
- 在保持 `unknown_copyright_policy: metadata_only` 边界下，未默认保存完整新闻 HTML；为 `qdc_silver.news` 新增 `body_text`、`body_download_status`、`body_error_message`、`body_size_bytes`，用于保存公开新闻页抽取后的短正文预览文本。
- `eastmoney_roll_news` 在完成标题/URL 到标的映射后，只对已映射新闻 URL 抓取页面正文，抽取 `ContentBody` 文本并截断为本地预览；document bundle 的 `records.jsonl` 同步写入正文预览。
- 控制台文档明细识别 `body_text` 后显示 `local_text` 和“已保存新闻正文预览文本”，弹窗中直接展示正文预览；没有正文时仍回退到元数据 JSON。
- 文本因子构建在存在 `body_text` 时把正文预览传给文本分类器；没有正文时继续只使用标题。
- 已对 `2026-05-13` daily-only 样本补抓正文预览：`SH600604` 市北高新 2 条新闻、`SH601991` 大唐发电 3 条新闻、`SZ300965` 恒宇信通 1 条新闻均已具备本地正文预览。

## 2026-05-13 今日样本多源复查

- 按用户要求复查新闻/公告是否支持多来源展示；控制台文档明细本身支持 `source_ids` 合并展示。
- 对 `SH600604`、`SH601991`、`SZ300965` 补跑 `sse_announcement`、`sina_finance_news`、`nbd_company_news`。
- `sse_announcement` 成功新增 2 条公告，沪市样本 `SH600604`、`SH601991` 的公告明细已合并显示 `cninfo_announcement, sse_announcement`。
- `sina_finance_news` 任务成功但 provider 返回 0 条记录；`nbd_company_news` 仍因 `www.nbd.com.cn` SSL EOF 失败。因此当前今日样本新闻仍只有 `eastmoney_roll_news`。
- 重建 `2026-05-13` 文本因子并运行质量检查：`build-factors` 7 行，`quality` 检查 78 行、0 issue。
